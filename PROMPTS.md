# 프롬프트 & 리뷰 로그

AI 도구: Claude Code

기능 단위로 AI에 지시하고, 받은 출력을 검증한 결과를 남긴다.
`리뷰/검증` 항목에는 실제로 돌려본 테스트 출력과 브라우저 확인 결과만 적는다.

---

## [setup] 요구사항 분해와 작업 순서 결정

### 프롬프트 1

> 채용 파이프라인 보드를 만드는 과제를 받았다. 지문 전체를 아래에 붙인다.
>
> 채용 담당자가 지원자를 단계별로 관리하는 보드다. 단계는 서류검토 → 면접 → 처우협의 → 최종합격 / 불합격이고,
> 지원자 카드를 단계 간 이동하며 관리한다. 실제 백엔드는 없고 mock API를 통해 읽고 쓴다.
>
> 필수 요구사항(Must) 6가지:
> 1. 보드/컬럼/카드 — 단계별 컬럼에 지원자 카드를 표시. 카드에는 이름·직무·지원일·현재 단계
> 2. 단계 이동 — 드래그앤드롭 또는 명시적 액션 버튼 중 택1. 이동은 mock API에 저장되어 새로고침 후에도 유지
> 3. 낙관적 업데이트 — 이동 시 UI를 먼저 반영하고 API 실패 시 원상 복구 + 사용자 피드백
> 4. 검색/필터 — 이름 검색 + 직무별 필터. 200건 이상에서도 체감 지연 없이
> 5. 상세 보기 — 카드 클릭 시 사이드 패널 또는 모달
> 6. 로딩/에러/빈 상태 — 세 가지 모두 UI로 처리
>
> 도전 요구사항(Should, 가점): 경쟁 상태 처리(같은 카드 빠른 연속 이동), 대량 데이터 성능(1,000건 가상 스크롤 등),
> 되돌리기, 핵심 로직 테스트, 키보드만으로 단계 이동·상세 열람.
>
> 기술 제약: 스택 자유. mock API는 네트워크 지연 200~800ms와 실패 약 15%를 시뮬레이션하되 자체 구현.
> 외부 UI 라이브러리는 가능하지만 핵심 상태 로직을 통째로 대신하는 라이브러리를 쓰면 그 이유를 문서에 밝혀야 한다.
>
> 평가는 기능 완성도보다 진행 과정을 본다. 커밋 하나에 기능 하나로 쪼개야 하고, 몰아서 커밋하면 감점이다.
>
> 바로 코드 짜지 말고 이것부터 해줘. 위 요구사항을 작업 가능한 기능 단위로 쪼개고 ID를 붙인다.
> 커밋 scope와 ID가 1:1로 붙어야 한다. 그리고 구현 순서를 의존 관계 기준으로 정해줘 —
> 지문 순서대로 만들면 나중에 뜯어고치게 될 지점이 있으면 미리 짚어줘.

### AI 출력 요지

Must 6개 + Should 5개 + 기술 제약 1개를 12개 단위로 쪼개고 `M1~M6 / S1~S5 / C1` ID를 붙였다.
지문 순서와 다르게 배치해야 하는 지점으로 네 가지를 짚었다.

- `C1`(mock API)이 맨 앞. 나머지 기능이 얹힐 바닥이라 이게 없으면 시작할 수 없다
- `M6`(로딩/에러/빈 상태)을 `M2`(이동)보다 앞으로. 데이터를 가져오는 순간 세 상태가 이미 생긴다
- `S1`(경쟁 상태)과 `S4`(테스트)를 `M3`(낙관적 업데이트) 바로 뒤에. 같은 로직을 건드리므로
  도전 과제라는 이유로 뒤로 미루면 낙관적 업데이트를 두 번 짜게 된다
- `M4`(검색/필터)를 `M2`·`M3` 뒤로. 필터가 걸린 상태에서 카드를 이동시키면 화면 순서와 원본 순서가 어긋난다

### 리뷰 / 검증

- **채택 — `M3` 직후 `S1` 배치**: 두 기능이 정말 같은 문제를 푸는지 직접 따져봤다.
  낙관적 업데이트는 "아직 확정되지 않은 상태를 화면에 먼저 보여준다"이고,
  경쟁 상태는 "확정되지 않은 상태가 여러 개 겹친다"다.
  둘 다 *서버가 확정한 값과 화면에 보여주는 값을 분리해서 들고 있어야* 풀린다.
  나중에 붙이면 자료구조를 다시 설계하게 된다는 지적이 맞다고 봤다.
- **채택 — `S2`(가상 스크롤)를 맨 뒤로**: 렌더링 구조를 바꾸는 작업이라 기능이 다 들어간 뒤에 얹어야
  기존 동작이 깨졌는지 판단할 수 있다.
- **수정 — 문서 작성 시점**: AI는 설계 결정을 미리 `DECISIONS.md`에 몰아서 적어두자고 했다.
  그러면 아직 만들지도 않은 기능의 결정이 먼저 기록되어 실제 진행과 어긋난다.
  **결정이 실제로 필요해지는 시점에 한 항목씩 추가하는 방식**으로 바꿨다.
  스택은 스캐폴딩할 때, 이동 방식은 `M2`를 만들 때 정한다.
- **보류**: 배포(Vercel)는 지문에서 선택 항목이라 Must를 끝낸 뒤 판단하기로 했다.

---

## [scaffold] 프로젝트 스캐폴딩

### 프롬프트 2

> 스택을 정하고 프로젝트를 세우자. 지문은 스택 자유고 Next.js도 언급돼 있는데,
> 이 과제 구조에서 Next.js가 실익이 있는지 판단해줘. mock API를 브라우저 안에서 자체 구현해야 하는 조건을 감안해서.
>
> 정하고 나면 스캐폴딩하고, 템플릿이 만들어주는 데모 코드는 전부 걷어내라.
> 지문 감점 신호에 "미사용 코드 방치"가 있다.

### AI 출력 요지

Vite + React + TypeScript를 권했다. 근거는 이 과제에 서버가 없다는 것.
mock API가 지연·실패·영속을 전부 브라우저 안에서 흉내 내는 구조라 서버에서 미리 그릴 화면이 없고,
App Router를 쓰면 서버 컴포넌트 경계와 캐시 무효화라는 다룰 개념만 늘어난다고 했다.

스타일링은 외부 UI 라이브러리 없이 순수 CSS를 제안했다. 키보드 접근성이 도전 요구사항인데
컴포넌트 라이브러리를 쓰면 초점 관리가 라이브러리 안으로 숨는다는 이유였다.

### 리뷰 / 검증

- **기각 — `strict` 옵션 추가**: 생성된 `tsconfig.app.json`에 `"strict": true`가 없어서 추가하려 했다.
  추측으로 넣지 않고 실제로 확인했다. 임시 파일에 널 접근과 암시적 `any`를 넣고 `tsc`를 돌렸다.

  ```
  src/__strict-probe.ts(3,10): error TS18047: 'value' is possibly 'null'.
  src/__strict-probe.ts(5,29): error TS7006: Parameter 'x' implicitly has an 'any' type.
  ```

  `strict` 없이도 둘 다 잡힌다. TypeScript 6부터 기본값이 켜져 있어 설정을 추가할 필요가 없었다.
  확인 후 임시 파일은 지웠다. **없어도 되는 설정을 "안전하니까" 넣는 것도 결국 설명 못 할 코드가 된다고 보고 넣지 않았다.**
- **수정 — 데모 파일 정리 범위**: AI는 `App.tsx`와 `App.css`만 정리했다.
  실제로 템플릿이 만든 파일을 훑어보니 `public/favicon.svg`가 Vite 로고였고,
  `public/icons.svg`는 데모 App에서만 참조하던 아이콘 스프라이트였다.
  파비콘이 Vite 로고로 남으면 제출물로서 부적절하다. 다만 파비콘 자체를 없애면 실제 브라우저에서 `/favicon.ico` 404가 뜨므로,
  단계별 컬럼 세 개를 형상화한 SVG를 직접 그려 교체했다. `public/icons.svg`는 참조하는 곳이 없어 삭제했다.
  `src/index.css`도 111줄짜리 데모 스타일이라 색상 토큰과 기본 리셋만 남기고 다시 썼다.
- **확인한 것**: `tsconfig.app.json`에 `noUnusedLocals`, `noUnusedParameters`가 이미 켜져 있다.
  "미사용 코드 방치"를 컴파일 단계에서 막아주므로 그대로 두기로 했다.
- **검증**: 타입 검사·빌드·린트·실제 브라우저를 모두 돌렸다.

  ```
  npx tsc -b        통과
  npm run build     ✓ built in 499ms
  npm run lint      oxlint 경고 없음

  PASS  h1 렌더 / lang 속성(ko) / title / 콘솔 에러 없음   4/4 passed
  ```

---

## [mock-api] 지연·실패·영속을 시뮬레이션하는 mock API

### 프롬프트 3

> 요구사항 C1을 만들자. mock API를 자체 구현한다. MSW 같은 라이브러리는 쓰지 않는다 — 지문이 자체 구현을 명시했다.
>
> 조건은 이렇다.
> - 네트워크 지연 200~800ms
> - 실패 약 15%
> - 이동 결과가 새로고침 후에도 유지 (localStorage)
> - 지원자 1,000건 시드. 도전 과제에 1,000건 성능이 있으니 처음부터 그 규모로 간다
>
> 시드 데이터는 매번 같아야 한다. 테스트에서 "1,000건 중 몇 건" 같은 단언을 쓸 거라
> `Math.random`으로 만들면 안 된다.
>
> 단계 분포는 실제 채용 파이프라인처럼 앞 단계가 두껍게 쌓이도록 가중치를 줘라.
> 전 단계에 고르게 뿌리면 보드가 비현실적으로 보인다.
>
> 파일은 타입 / 시드 생성 / API 세 개로 나눠라.

### AI 출력 요지

`src/types.ts`(도메인 타입·단계 상수), `src/mock/seed.ts`(시드 생성), `src/mock/api.ts`(조회·이동) 세 파일로 나눴다.

시드는 선형 합동 생성기(LCG)에 고정 시드를 넣어 재현 가능하게 만들었고,
단계 가중치는 서류검토 0.42 / 면접 0.22 / 처우협의 0.10 / 최종합격 0.08 / 불합격 0.18을 제안했다.
지연은 `setTimeout`, 실패는 확률 비교, 저장은 localStorage.

### 리뷰 / 검증

- **문제 — 생성 데이터에 한글이 아닌 문자가 섞임**: 이름 음절 배열 `GIVEN_FIRST`에 `"准"`이 들어 있었다.
  중국어 간체자다. 그대로 뒀으면 1,000건 중 일부가 `임准연` 같은 이름으로 생성된다.
  - **어떻게 알아챘나**: 배열 원소를 눈으로 훑는 대신 정규식으로 전부 검사했다.
    `/^[가-힣]$/`를 만족하지 않는 원소를 뽑아내는 스크립트를 돌렸다.

    ```
    SURNAMES 15개 OK
    GIVEN_FIRST 15개 이상함: "准"
    GIVEN_SECOND 15개 OK
    ```
  - **판단**: 수정. 타입이 전부 `string`이라 **컴파일러도 린터도 잡지 못하는 종류의 결함**이다.
    생성 데이터는 타입 검사를 통과해도 내용이 틀릴 수 있으므로 별도로 확인해야 한다는 걸 이 지점에서 확인했다.
- **직접 판단한 것 — 실패율을 어디에 적용할까**: 지문은 "실패 약 15%"라고만 하고 조회·쓰기 구분이 없다.
  조회에도 걸면 보드를 여는 것 자체가 도박이 되어 평가자가 기능을 확인할 수 없다고 봤다.
  쓰기에만 적용하고 조회 실패율은 따로 분리해 기본 0으로 뒀다. 가정이므로 [DECISIONS.md](DECISIONS.md) D2에 명시했다.
- **추가한 것 — 저장소 키 버전**: `worksphere:applicants:v1`.
  시드 로직을 바꿨는데 브라우저에 예전 데이터가 남아 있으면 새 코드 동작을 확인할 수 없다.
  실제로 개발 중에 겪을 문제라 처음부터 키에 버전을 붙였다.
- **검증은 다음 커밋에서 한꺼번에**: 이 커밋에서 확인해야 할 것이 세 가지인데 셋 다 손으로는 제대로 못 본다.
  실패율은 100번 눌러 13번 실패해도 그게 15%인지 우연인지 알 수 없고,
  지연은 매번 달라져 눈대중으로 범위를 판단할 수 없고,
  단계 분포는 1,000건을 세어야 한다.
  전부 수백 회 시행과 집계가 필요해서, 콘솔에서 일회성으로 확인하는 대신 테스트 환경을 세워 회귀 가능한 형태로 만들기로 했다.
  구현과 검증을 한 커밋에 섞지 않고 나눈 이유이기도 하다.
