# 프롬프트 & 리뷰 로그

AI 도구: Claude Code

기능 단위로 AI에 지시하고, 받은 출력을 검증한 결과를 남긴다.
`리뷰/검증` 항목에는 실제로 돌려본 테스트 출력과 브라우저 확인 결과만 적는다.

---

## [setup] 요구사항 분해와 작업 순서 결정

### 프롬프트 1

> 채용 파이프라인 보드를 만드는 과제를 받았다. 지문 전체를 아래에 붙인다.
>
> 채용 담당자가 지원자를 단계별로 관리하는 보드다. 단계는 서류검토 → 면접 → 처우협의 → 최종합격 / 불합격이고,
> 지원자 카드를 단계 간 이동하며 관리한다. 실제 백엔드는 없고 mock API를 통해 읽고 쓴다.
>
> 필수 요구사항(Must) 6가지:
> 1. 보드/컬럼/카드 — 단계별 컬럼에 지원자 카드를 표시. 카드에는 이름·직무·지원일·현재 단계
> 2. 단계 이동 — 드래그앤드롭 또는 명시적 액션 버튼 중 택1. 이동은 mock API에 저장되어 새로고침 후에도 유지
> 3. 낙관적 업데이트 — 이동 시 UI를 먼저 반영하고 API 실패 시 원상 복구 + 사용자 피드백
> 4. 검색/필터 — 이름 검색 + 직무별 필터. 200건 이상에서도 체감 지연 없이
> 5. 상세 보기 — 카드 클릭 시 사이드 패널 또는 모달
> 6. 로딩/에러/빈 상태 — 세 가지 모두 UI로 처리
>
> 도전 요구사항(Should, 가점): 경쟁 상태 처리(같은 카드 빠른 연속 이동), 대량 데이터 성능(1,000건 가상 스크롤 등),
> 되돌리기, 핵심 로직 테스트, 키보드만으로 단계 이동·상세 열람.
>
> 기술 제약: 스택 자유. mock API는 네트워크 지연 200~800ms와 실패 약 15%를 시뮬레이션하되 자체 구현.
> 외부 UI 라이브러리는 가능하지만 핵심 상태 로직을 통째로 대신하는 라이브러리를 쓰면 그 이유를 문서에 밝혀야 한다.
>
> 평가는 기능 완성도보다 진행 과정을 본다. 커밋 하나에 기능 하나로 쪼개야 하고, 몰아서 커밋하면 감점이다.
>
> 바로 코드 짜지 말고 이것부터 해줘. 위 요구사항을 작업 가능한 기능 단위로 쪼개고 ID를 붙인다.
> 커밋 scope와 ID가 1:1로 붙어야 한다. 그리고 구현 순서를 의존 관계 기준으로 정해줘 —
> 지문 순서대로 만들면 나중에 뜯어고치게 될 지점이 있으면 미리 짚어줘.

### AI 출력 요지

Must 6개 + Should 5개 + 기술 제약 1개를 12개 단위로 쪼개고 `M1~M6 / S1~S5 / C1` ID를 붙였다.
지문 순서와 다르게 배치해야 하는 지점으로 네 가지를 짚었다.

- `C1`(mock API)이 맨 앞. 나머지 기능이 얹힐 바닥이라 이게 없으면 시작할 수 없다
- `M6`(로딩/에러/빈 상태)을 `M2`(이동)보다 앞으로. 데이터를 가져오는 순간 세 상태가 이미 생긴다
- `S1`(경쟁 상태)과 `S4`(테스트)를 `M3`(낙관적 업데이트) 바로 뒤에. 같은 로직을 건드리므로
  도전 과제라는 이유로 뒤로 미루면 낙관적 업데이트를 두 번 짜게 된다
- `M4`(검색/필터)를 `M2`·`M3` 뒤로. 필터가 걸린 상태에서 카드를 이동시키면 화면 순서와 원본 순서가 어긋난다

### 리뷰 / 검증

- **채택 — `M3` 직후 `S1` 배치**: 두 기능이 정말 같은 문제를 푸는지 직접 따져봤다.
  낙관적 업데이트는 "아직 확정되지 않은 상태를 화면에 먼저 보여준다"이고,
  경쟁 상태는 "확정되지 않은 상태가 여러 개 겹친다"다.
  둘 다 *서버가 확정한 값과 화면에 보여주는 값을 분리해서 들고 있어야* 풀린다.
  나중에 붙이면 자료구조를 다시 설계하게 된다는 지적이 맞다고 봤다.
- **채택 — `S2`(가상 스크롤)를 맨 뒤로**: 렌더링 구조를 바꾸는 작업이라 기능이 다 들어간 뒤에 얹어야
  기존 동작이 깨졌는지 판단할 수 있다.
- **수정 — 문서 작성 시점**: AI는 설계 결정을 미리 `DECISIONS.md`에 몰아서 적어두자고 했다.
  그러면 아직 만들지도 않은 기능의 결정이 먼저 기록되어 실제 진행과 어긋난다.
  **결정이 실제로 필요해지는 시점에 한 항목씩 추가하는 방식**으로 바꿨다.
  스택은 스캐폴딩할 때, 이동 방식은 `M2`를 만들 때 정한다.
- **보류**: 배포(Vercel)는 지문에서 선택 항목이라 Must를 끝낸 뒤 판단하기로 했다.

---

## [scaffold] 프로젝트 스캐폴딩

### 프롬프트 2

> 스택을 정하고 프로젝트를 세우자. 지문은 스택 자유고 Next.js도 언급돼 있는데,
> 이 과제 구조에서 Next.js가 실익이 있는지 판단해줘. mock API를 브라우저 안에서 자체 구현해야 하는 조건을 감안해서.
>
> 정하고 나면 스캐폴딩하고, 템플릿이 만들어주는 데모 코드는 전부 걷어내라.
> 지문 감점 신호에 "미사용 코드 방치"가 있다.

### AI 출력 요지

Vite + React + TypeScript를 권했다. 근거는 이 과제에 서버가 없다는 것.
mock API가 지연·실패·영속을 전부 브라우저 안에서 흉내 내는 구조라 서버에서 미리 그릴 화면이 없고,
App Router를 쓰면 서버 컴포넌트 경계와 캐시 무효화라는 다룰 개념만 늘어난다고 했다.

스타일링은 외부 UI 라이브러리 없이 순수 CSS를 제안했다. 키보드 접근성이 도전 요구사항인데
컴포넌트 라이브러리를 쓰면 초점 관리가 라이브러리 안으로 숨는다는 이유였다.

### 리뷰 / 검증

- **기각 — `strict` 옵션 추가**: 생성된 `tsconfig.app.json`에 `"strict": true`가 없어서 추가하려 했다.
  추측으로 넣지 않고 실제로 확인했다. 임시 파일에 널 접근과 암시적 `any`를 넣고 `tsc`를 돌렸다.

  ```
  src/__strict-probe.ts(3,10): error TS18047: 'value' is possibly 'null'.
  src/__strict-probe.ts(5,29): error TS7006: Parameter 'x' implicitly has an 'any' type.
  ```

  `strict` 없이도 둘 다 잡힌다. TypeScript 6부터 기본값이 켜져 있어 설정을 추가할 필요가 없었다.
  확인 후 임시 파일은 지웠다. **없어도 되는 설정을 "안전하니까" 넣는 것도 결국 설명 못 할 코드가 된다고 보고 넣지 않았다.**
- **수정 — 데모 파일 정리 범위**: AI는 `App.tsx`와 `App.css`만 정리했다.
  실제로 템플릿이 만든 파일을 훑어보니 `public/favicon.svg`가 Vite 로고였고,
  `public/icons.svg`는 데모 App에서만 참조하던 아이콘 스프라이트였다.
  파비콘이 Vite 로고로 남으면 제출물로서 부적절하다. 다만 파비콘 자체를 없애면 실제 브라우저에서 `/favicon.ico` 404가 뜨므로,
  단계별 컬럼 세 개를 형상화한 SVG를 직접 그려 교체했다. `public/icons.svg`는 참조하는 곳이 없어 삭제했다.
  `src/index.css`도 111줄짜리 데모 스타일이라 색상 토큰과 기본 리셋만 남기고 다시 썼다.
- **확인한 것**: `tsconfig.app.json`에 `noUnusedLocals`, `noUnusedParameters`가 이미 켜져 있다.
  "미사용 코드 방치"를 컴파일 단계에서 막아주므로 그대로 두기로 했다.
- **검증**: 타입 검사·빌드·린트·실제 브라우저를 모두 돌렸다.

  ```
  npx tsc -b        통과
  npm run build     ✓ built in 499ms
  npm run lint      oxlint 경고 없음

  PASS  h1 렌더 / lang 속성(ko) / title / 콘솔 에러 없음   4/4 passed
  ```

---

## [mock-api] 지연·실패·영속을 시뮬레이션하는 mock API

### 프롬프트 3

> 요구사항 C1을 만들자. mock API를 자체 구현한다. MSW 같은 라이브러리는 쓰지 않는다 — 지문이 자체 구현을 명시했다.
>
> 조건은 이렇다.
> - 네트워크 지연 200~800ms
> - 실패 약 15%
> - 이동 결과가 새로고침 후에도 유지 (localStorage)
> - 지원자 1,000건 시드. 도전 과제에 1,000건 성능이 있으니 처음부터 그 규모로 간다
>
> 시드 데이터는 매번 같아야 한다. 테스트에서 "1,000건 중 몇 건" 같은 단언을 쓸 거라
> `Math.random`으로 만들면 안 된다.
>
> 단계 분포는 실제 채용 파이프라인처럼 앞 단계가 두껍게 쌓이도록 가중치를 줘라.
> 전 단계에 고르게 뿌리면 보드가 비현실적으로 보인다.
>
> 파일은 타입 / 시드 생성 / API 세 개로 나눠라.

### AI 출력 요지

`src/types.ts`(도메인 타입·단계 상수), `src/mock/seed.ts`(시드 생성), `src/mock/api.ts`(조회·이동) 세 파일로 나눴다.

시드는 선형 합동 생성기(LCG)에 고정 시드를 넣어 재현 가능하게 만들었고,
단계 가중치는 서류검토 0.42 / 면접 0.22 / 처우협의 0.10 / 최종합격 0.08 / 불합격 0.18을 제안했다.
지연은 `setTimeout`, 실패는 확률 비교, 저장은 localStorage.

### 리뷰 / 검증

- **문제 — 생성 데이터에 한글이 아닌 문자가 섞임**: 이름 음절 배열 `GIVEN_FIRST`에 `"准"`이 들어 있었다.
  중국어 간체자다. 그대로 뒀으면 1,000건 중 일부가 `임准연` 같은 이름으로 생성된다.
  - **어떻게 알아챘나**: 배열 원소를 눈으로 훑는 대신 정규식으로 전부 검사했다.
    `/^[가-힣]$/`를 만족하지 않는 원소를 뽑아내는 스크립트를 돌렸다.

    ```
    SURNAMES 15개 OK
    GIVEN_FIRST 15개 이상함: "准"
    GIVEN_SECOND 15개 OK
    ```
  - **판단**: 수정. 타입이 전부 `string`이라 **컴파일러도 린터도 잡지 못하는 종류의 결함**이다.
    생성 데이터는 타입 검사를 통과해도 내용이 틀릴 수 있으므로 별도로 확인해야 한다는 걸 이 지점에서 확인했다.
- **직접 판단한 것 — 실패율을 어디에 적용할까**: 지문은 "실패 약 15%"라고만 하고 조회·쓰기 구분이 없다.
  조회에도 걸면 보드를 여는 것 자체가 도박이 되어 평가자가 기능을 확인할 수 없다고 봤다.
  쓰기에만 적용하고 조회 실패율은 따로 분리해 기본 0으로 뒀다. 가정이므로 [DECISIONS.md](DECISIONS.md) D2에 명시했다.
- **추가한 것 — 저장소 키 버전**: `worksphere:applicants:v1`.
  시드 로직을 바꿨는데 브라우저에 예전 데이터가 남아 있으면 새 코드 동작을 확인할 수 없다.
  실제로 개발 중에 겪을 문제라 처음부터 키에 버전을 붙였다.
- **검증은 다음 커밋에서 한꺼번에**: 이 커밋에서 확인해야 할 것이 세 가지인데 셋 다 손으로는 제대로 못 본다.
  실패율은 100번 눌러 13번 실패해도 그게 15%인지 우연인지 알 수 없고,
  지연은 매번 달라져 눈대중으로 범위를 판단할 수 없고,
  단계 분포는 1,000건을 세어야 한다.
  전부 수백 회 시행과 집계가 필요해서, 콘솔에서 일회성으로 확인하는 대신 테스트 환경을 세워 회귀 가능한 형태로 만들기로 했다.
  구현과 검증을 한 커밋에 섞지 않고 나눈 이유이기도 하다.

---

## [mock-api] 테스트 환경 구성과 계약 검증

### 프롬프트 4

> 앞 커밋에서 만든 mock API를 테스트로 검증하자. 테스트 환경부터 세워라. Vite를 쓰고 있으니 vitest.
> localStorage를 쓰니까 DOM 환경이 필요할 거다.
>
> 확인할 것은 세 가지다.
> - 지연이 정말 200~800ms 안에 들어가는가
> - 쓰기 실패율이 정말 15% 근처인가
> - 이동이 저장되고, **실패했을 때는 저장소가 안 바뀌는가**
>
> 세 번째가 중요하다. 다음에 만들 낙관적 업데이트 롤백이 그 성질에 기댄다.
> 서버가 실패했는데 일부라도 썼다면 롤백한 화면과 저장 상태가 어긋난다.
>
> 그리고 지난번에 이름 배열에 한글 아닌 문자가 섞였던 것, 그게 다시 들어오면 잡히게 테스트로 막아라.
> 단계 분포 가중치도 의도대로 나오는지 확인하고.

### AI 출력 요지

`vitest` + `jsdom`만 설치하고 `vite.config.ts`에 `test.environment: 'jsdom'`을 추가했다.
테스트는 두 파일로 나눴다. `seed.test.ts`는 생성 데이터의 성질을, `api.test.ts`는 API의 계약을 검증한다.

확률·시간을 다루는 테스트는 `MOCK_CONFIG`의 지연을 0으로 낮춰 수백 회를 빠르게 시행하는 방식을 썼다.

### 리뷰 / 검증

- **직접 판단한 것 — 허용 오차를 어디까지 둘까**: "실패율이 15% 근처"를 어떻게 단언할지가 문제였다.
  범위를 좁게 잡으면 간헐적으로 실패하는 테스트가 되고, 넓게 잡으면 검증이 무의미해진다.

  600회 시행에서 표준편차는 `√(0.15 × 0.85 / 600) ≈ 0.0146`이다.
  허용 범위를 ±5%p(0.10 ~ 0.20)로 두면 약 3.4σ에 해당해 우연히 벗어날 확률이 0.1% 아래다.
  검증은 되면서 간헐적 실패는 나지 않는 지점이라고 보고 이 범위로 정했다.
- **확인한 것 — 정말 간헐적으로 실패하지 않는가**: 근거만 믿지 않고 5회 연속 실행했다.

  ```
  회차 1: Tests  16 passed (16)
  회차 2: Tests  16 passed (16)
  회차 3: Tests  16 passed (16)
  회차 4: Tests  16 passed (16)
  회차 5: Tests  16 passed (16)
  ```
- **추가한 것 — 회귀 테스트**: 앞 커밋에서 이름 음절 배열에 한글이 아닌 문자가 섞였던 문제를 테스트로 막았다.
  `/^[가-힣]{3}$/`를 만족하지 않는 항목이 하나라도 있으면 실패한다.
  타입이 전부 `string`이라 컴파일러가 잡지 못하는 종류라, 같은 실수가 다시 들어오면 여기서 걸린다.
- **추가한 것 — 손상된 저장소 값**: AI 초안에는 없던 경우다.
  `readStore`에 `JSON.parse` 실패를 처리하는 분기가 있는데 그 경로를 아무도 밟지 않고 있었다.
  깨진 문자열을 localStorage에 직접 넣고 조회해서, 버리고 다시 시딩하는지 확인하는 테스트를 넣었다.
  **코드에 있는 분기는 테스트로 한 번은 밟아봐야 정말 동작하는지 알 수 있다.**
- **수정 — 지연 테스트가 한 번만 재고 있었다**: AI 초안은 `fetchApplicants()`를 한 번 호출해 걸린 시간이 범위 안인지 봤다.
  다시 읽어보니 이건 "이번 호출이 범위 안이었다"를 확인할 뿐 **"지연이 200~800ms 범위다"를 확인하지 못한다.**
  지연이 늘 정확히 200ms로 고정되도록 잘못 바뀌어도 통과한다.
  8회 샘플링해서 최솟값·최댓값이 범위 안인지, 그리고 최대-최소 차가 50ms를 넘는지(고정값이 아닌지)까지 단언하도록 바꿨다.
- **수정 — 저장소 키를 문자열로 박아둔 테스트**: 손상된 값 테스트가 `"worksphere:applicants:v1"` 문자열을 직접 쓰고 있었다.
  앞 커밋에서 "시드 로직이 바뀌면 키 버전을 올린다"고 정해놓고, 정작 그 상황에서 이 테스트는
  **엉뚱한 키에 쓰고 통과해 조용히 무력화된다.** 실패하지 않으니 알아챌 방법도 없다.
  `STORAGE_KEY`를 `export`해서 테스트가 같은 상수를 쓰도록 고쳤다.
- **검증 결과**: `npm test`로 실행한다. 아래는 테스트 이름까지 보려고 `npx vitest run --reporter=verbose`로 뽑은 출력이다.

  ```
  ✓ 시드 생성 > 요청한 개수만큼 만들고 id가 겹치지 않는다
  ✓ 시드 생성 > 같은 시드면 같은 데이터가 나온다
  ✓ 시드 생성 > 다른 시드면 다른 데이터가 나온다
  ✓ 시드 생성 > 이름이 한글 세 글자다
  ✓ 시드 생성 > 지원일이 YYYY-MM-DD 형식이고 미래가 아니다
  ✓ 시드 생성 > 단계와 직무가 정의된 값만 쓴다
  ✓ 시드 생성 > 단계 분포가 의도한 가중치를 따른다
  ✓ 시드 생성 > 직무가 한쪽으로 쏠리지 않는다
  ✓ 조회 > 지연이 200~800ms 범위에서 매번 달라진다        3884ms
  ✓ 조회 > 기본값으로는 실패하지 않는다                   102ms
  ✓ 조회 > 조회 실패율을 올리면 ApiError를 던진다
  ✓ 조회 > 저장된 값이 깨져 있으면 버리고 다시 시딩한다
  ✓ 단계 이동 > 실패율이 15% 근처다                      1573ms
  ✓ 단계 이동 > 성공하면 저장되어 다시 조회해도 유지된다
  ✓ 단계 이동 > 실패하면 저장소를 건드리지 않는다
  ✓ 단계 이동 > 없는 지원자를 옮기려 하면 ApiError를 던진다

  Test Files  2 passed (2)
       Tests  16 passed (16)
  ```

  앞 커밋에서 "수백 회 시행이 필요해 미룬다"고 했던 지연·실패율·분포가 전부 여기서 잡혔다.
  콘솔에서 일회성으로 보는 것과 달리, 이후 mock API를 고칠 때 깨지면 바로 알 수 있다.

---

## [board-layout] 단계별 컬럼과 지원자 카드

### 프롬프트 5

> M1을 만들자. 5단계 컬럼에 지원자 카드를 표시한다. 카드에는 이름·직무·지원일·현재 단계가 들어가야 한다.
>
> 상태는 `useReducer`로 간다. 지금은 조회만 필요한데, 곧 단계 이동과 낙관적 업데이트가 붙는다는 걸 감안해서
> 자료구조를 잡아라. 같은 카드를 연속으로 옮기는 경우도 요구사항에 있다.
>
> 접근성이 도전 요구사항에 있으니 마크업을 처음부터 시맨틱하게 짜라.
> 나중에 `div`를 걷어내고 다시 짜는 것보다 지금 제대로 두는 게 싸다.
>
> 로딩·에러 상태 UI는 다음 커밋에서 할 거니까 지금은 넣지 마라. 커밋을 섞지 않는다.

### AI 출력 요지

상태는 `byId`(id 맵) + `order`(순서 배열) 정규화 구조를 제안했다.
배열로 두면 카드 한 장을 옮길 때마다 1,000개를 순회해 새 배열을 만들어야 한다는 이유였다.

컴포넌트는 `Board`(단계별 그룹핑) → `Column`(`section` + `h2` + `ul`) → `ApplicantCard`(`li`) 3단으로 나눴다.
그룹핑은 `useMemo`로 `Map`을 한 번만 만들고, 컬럼은 `aria-labelledby`로 제목과 연결했다.

### 리뷰 / 검증

- **채택 — 정규화 구조**: 지금 필요한 건 조회뿐이라 배열이 더 단순하다. 그런데도 받아들인 이유는
  단건 갱신이 이 화면의 주된 동작이기 때문이다. 이동이 핵심 기능이고 연속 이동까지 요구사항에 있다.
  나중에 바꾸면 이동·롤백 로직을 다시 쓰게 된다고 보고 지금 잡았다. [DECISIONS.md](DECISIONS.md) D3에 근거를 남겼다.
- **직접 따져본 것 — 그룹핑 메모가 실제로 동작하는가**: `Board`의 `useMemo` 의존성이 `applicants` 배열 하나인데,
  `App`에서 `state.order.map(...)`으로 매번 새 배열을 만들면 참조가 달라져 메모가 무의미해진다.
  AI 초안이 그 상태였다. `App` 쪽에도 `useMemo([state.order, state.byId])`를 걸어
  상태가 그대로면 같은 배열 참조가 유지되도록 고쳤다.
- **확인한 것 — 마크업이 실제로 시맨틱한가**: 클래스 이름만 그럴듯하고 태그는 전부 `div`인 경우가 흔하다.
  브라우저에서 실제 태그와 `aria-labelledby` 연결을 직접 확인했다.

  ```
  PASS  컬럼 시맨틱 → { tag: "SECTION", labelled: true, listTag: "UL", itemTag: "LI" }
  ```
- **검증 — 실제 브라우저**: 빌드 결과를 Chromium으로 열어 DOM을 직접 셌다.

  ```
  PASS  컬럼 5개 → ["서류검토","면접","처우협의","최종합격","불합격"]
  PASS  카드 총 1000장
  PASS  분포 합계 = 1000     단계 분포: 437 / 226 / 86 / 81 / 170
  PASS  이름 한글 3자 / 지원일 표시 / 현재 단계 표시
  PASS  요약 인원수 → "지원자 1000명"
  PASS  콘솔 에러 없음
        첫 카드: {"name":"임태연","role":"QA","meta":"지원일 2026-06-11서류검토"}
        9/9 passed
  ```

  컬럼별 합계가 1,000과 맞으므로 그룹핑에서 누락되는 카드가 없다는 뜻이기도 하다.
- **남긴 문제 — 1,000장을 전부 DOM에 그린다**: 가상 스크롤(S2)로 해결할 항목이라 지금은 두되,
  나중에 비교할 기준선을 먼저 재뒀다.

  ```
  DOM 노드 6,034개
  스크롤 프레임 중앙값 8.3ms / 최대 311.9ms
  ```

  중앙값은 문제없지만 최대 311ms 스파이크가 있다. 지금 미리 최적화하면 이동·롤백을 붙일 때 방해가 되므로
  기능을 다 넣은 뒤 `virtualization`(S2)에서 이 숫자와 비교한다.

---

## [loading-error-empty] 로딩·에러·빈 상태

### 프롬프트 6

> M6을 만들자. 로딩·에러·빈 상태 세 가지를 UI로 처리한다.
>
> 문제가 하나 있다. **에러와 빈 상태는 지금 재현할 방법이 없다.** 조회 실패율은 기본 0이고 시드는 항상 1,000건이다.
> 구현해도 평가자가 확인할 수 없으면 의미가 없으니, 세 상태를 전환시킬 수 있는 시연용 조작 장치도 같이 만들어라.
> 대신 제품 기능이 아니라는 게 한눈에 보여야 한다.
>
> 로딩은 문구만 띄우지 말고 컬럼 골격을 보여줘라. 지연이 200~800ms라 데이터가 오는 순간 화면이 통째로 바뀌면 튄다.
>
> 에러 상태에는 재시도 버튼을 넣는다.

### AI 출력 요지

`BoardSkeleton` / `BoardError` / `BoardEmpty` 세 컴포넌트와 `DemoControls` 패널을 만들었다.
mock API에는 `clearStore`(빈 목록으로 만들기)와 `resetStore`(다시 시딩)를 추가했다.

스켈레톤은 컬럼 다섯 개 골격에 카드 자리를 회색 블록으로 채우고, 에러는 `role="alert"`,
상태 문구는 `aria-live="polite"` 영역에 넣었다.

### 리뷰 / 검증

- **직접 판단한 것 — 시연 패널을 남길 것인가**: 실제 제품이라면 빼야 할 코드다.
  그런데 이 과제는 mock 환경 자체가 요구사항이고, 구현한 상태를 확인할 수 없으면 평가가 안 된다.
  남기되 배경색과 "시연용" 표시로 제품 기능과 구분되게 했다. 근거는 [DECISIONS.md](DECISIONS.md) D4.
- **확인한 것 — 스켈레톤이 보조기기에 읽히지 않는가**: 읽을 내용이 없는 골격을 스크린리더가 읽으면 방해만 된다.
  `aria-hidden="true"`로 감추고, 상태는 `aria-live` 영역의 문구(`불러오는 중`)로 따로 알리도록 했다.
- **추가한 것 — 움직임 줄이기**: 스켈레톤에 반짝임 애니메이션이 있어서 `prefers-reduced-motion`에서 끄도록 했다.
  AI 초안에는 없었다. 접근성이 도전 요구사항이라 초점 관리만이 아니라 이런 것도 범위에 든다고 봤다.
- **검증 — 상태 전환을 실제로 돌려봤다**: 정적으로 렌더되는지만 보면 의미가 없다.
  로딩 → 조회 실패 → 재시도 → 복구 → 비우기 → 빈 상태 → 되돌리기 순서로 브라우저에서 직접 전환시켰다.

  ```
  PASS  로딩: 스켈레톤 표시 / 요약 문구 "불러오는 중"
  PASS  ready: 카드 1000장 / 스켈레톤 사라짐
  PASS  에러: role=alert / 메시지 / 보드 사라짐
  PASS  재시도: 복구됨 (1000장)
  PASS  빈 상태: 문구 / 요약 "지원자 0명"
  PASS  되돌리기: 1000장 복구
  PASS  콘솔 에러 없음
        12/12 passed
  ```

  재시도 버튼이 **실제로 복구까지 되는지**를 따로 확인한 이유는, 버튼이 있지만 눌러도 같은 에러로 돌아오는 경우가 흔해서다.

---

## [stage-move] 액션 버튼으로 단계 이동

### 프롬프트 7

> M2를 만들자. 카드를 다른 단계로 이동시키고 mock API에 저장한다. 새로고침 후에도 유지돼야 한다.
>
> 드래그앤드롭 대신 **액션 버튼**으로 간다. 도전 요구사항에 "키보드만으로 단계 이동"이 있는데,
> 드래그를 택하면 키보드 대체 경로를 따로 만들어야 해서 사실상 두 벌을 짜게 된다. 버튼은 그 자체가 키보드 경로다.
>
> 주의할 점이 있다.
> - 불합격은 합격 경로의 끝이 아니라 어느 단계에서나 가는 분기다. 이전/다음과 같은 선상에 두지 마라
> - "이 단계에서 어디로 갈 수 있는가"는 순수 함수로 빼라. 컴포넌트 안에 조건문으로 흩어두면 테스트를 못 한다
> - 요청이 진행 중인 카드는 다시 못 누르게 막아라
>
> **낙관적 업데이트는 아직 하지 마라.** 지금은 서버가 확정한 뒤에 화면을 바꾼다. 그건 다음 커밋이다.

### AI 출력 요지

`getStageMoves(stage)`로 이동 규칙을 분리하고, 리듀서에 `move/start` · `move/success` · `move/failure` 세 액션을 추가했다.
진행 중인 카드는 `moving` 맵에 담아 버튼을 비활성화한다.

카드에는 `← 이전단계` / `다음단계 →` / `불합격` 버튼이 상황에 따라 붙는다.

### 리뷰 / 검증

- **직접 판단한 것 — 불합격 카드는 어디로 가는가**: AI 초안은 불합격 카드에 버튼을 하나도 두지 않았다.
  그러면 한 번 불합격 처리한 카드가 영영 움직이지 않는다. 시연 중에도 되돌릴 방법이 없다.
  불합격 직전 단계를 저장하지 않으므로 "이전 단계로"는 성립하지 않는다고 보고,
  경로의 첫 단계로 되돌리는 버튼만 두기로 했다. 가정이라 [DECISIONS.md](DECISIONS.md) D6에 남겼다.
- **수정 — 버튼 이름이 카드마다 구분되지 않는다**: AI 초안의 `aria-label`은 `"면접 단계로 이동"`이었다.
  화면에 카드가 1,000장이라 **같은 이름의 버튼이 수백 개 생긴다.** 스크린리더로 훑으면 어느 카드의 버튼인지 알 수 없다.
  지원자 이름을 앞에 붙여 `"임태연 지원자를 면접 단계로 이동"`으로 바꿨다.
- **확인한 것 — 단계별로 버튼이 맞게 나오는가**: 경우의 수가 셋이라 브라우저에서 전부 확인했다.

  ```
  서류검토 카드  → ["면접 →", "불합격"]           (이전 없음)
  최종합격 카드  → ["← 처우협의", "불합격"]        (다음 없음)
  불합격 카드    → ["← 서류검토"]                  (분기 밖)
  ```
- **검증 — 저장과 새로고침 유지**: M2의 핵심이 "새로고침 후에도 유지"라 거기까지 확인했다.
  쓰기 실패율이 15%라 성공할 때까지 눌렀다.

  ```
  PASS  이동 성공 시 컬럼 개수 변화
        이동 전: 437/226/86/81/170 → 이동 후: 436/227/86/81/170
  PASS  새로고침 후에도 유지
        새로고침 후: 436/227/86/81/170
  PASS  aria-label 있음 / 콘솔 에러 없음
        7/7 passed
  ```
- **남긴 문제 — 실패했을 때 아무 일도 일어나지 않는다**: 지금은 서버가 실패하면 버튼이 다시 활성화될 뿐 사용자에게 알리지 않는다.
  15% 확률로 눌러도 안 움직이는 것처럼 보인다. 낙관적 업데이트와 실패 피드백이 다음 커밋(M3)이라 의도적으로 비워뒀다.

### 프롬프트 8

구현을 끝내고 화면을 만져보다 이상한 점이 있었다. 카드를 옮겼는데 어디로 갔는지 눈에 띄지 않았다.
정렬 요구가 지문에 없어서 지금 동작이 무엇인지부터 확인하기로 했다.

> 카드를 면접이나 불합격으로 옮기면 대상 컬럼의 위로 가나, 아래로 가나?
> 정렬 기준 같은 게 지금 있는지, 아니면 나중에 넣어야 하는 건지 알려줘.

### AI 출력 요지

위도 아래도 아니라고 답했다. `order` 배열은 그대로 두고 `byId[id].stage`만 바꾸기 때문에
옮긴 카드가 **원래 id 순서상 제 자리**에 끼어든다는 설명이었다.
처음 확인한 카드(`A0001`)가 맨 위에 나타난 건 그게 `order`의 첫 번째 id여서 생긴 우연이었다.

### 리뷰 / 검증

- **확인한 것 — 설명이 맞는지 직접 측정**: 설명만 듣고 넘기지 않고 `order` 상 한참 뒤쪽 카드로 재현했다.

  ```
  서류검토 컬럼의 200번째 카드를 면접으로 이동
  → 면접 컬럼 227장 중 109번째에 들어감
  ```

  맨 위도 맨 아래도 아니었다. 스크롤을 한참 내려야 찾을 수 있다.
- **왜 그냥 둘 수 없었나**: 단순한 UX 불편으로 보였지만 따져보니 **다음 커밋을 막는 문제**였다.
  낙관적 업데이트는 "실패하면 카드가 되돌아온다"를 보여주는 게 요점인데,
  카드가 화면 밖에 있으면 롤백이 일어나도 확인할 방법이 없다. 제출용 화면 녹화도 불가능해진다.
- **판단**: 옮긴 카드를 대상 컬럼 맨 위로 보낸다. 지문에 정렬 요구가 없어 가정으로 처리하고
  [DECISIONS.md](DECISIONS.md) D6에 근거를 남겼다. 실제 칸반 보드도 최근 처리한 항목을 위에 둔다.
- **함께 고친 것 — 서버에도 같은 규칙**: 화면에서만 맨 위로 올리면 새로고침했을 때 다시 중간으로 내려간다.
  `updateStage`가 저장 배열에서도 해당 지원자를 앞으로 옮기게 했다. 실제 API가 `updated_at` 내림차순으로 주는 것과 같은 규칙이다.
  순서가 계약이 됐으므로 테스트도 추가했다 — 옮긴 지원자가 맨 앞에 오고, 순서만 바뀌고 사라지는 지원자는 없어야 한다.

  ```
  PASS  이동 직후 맨 위       (109번째 → 1번째)
  PASS  새로고침 후에도 맨 위
  PASS  카드 총수 유지 (1000)
  Tests  17 passed (17)
  ```
- **배운 것**: 요구사항에 적히지 않은 동작은 "아무 동작도 없음"이 아니라 **의도하지 않은 동작**으로 존재한다.
  정렬을 명시하지 않았다고 정렬이 없는 게 아니라, 자료구조가 정한 순서가 그대로 화면에 나온다.

---

## [mock-api] 지연 테스트가 기본 타임아웃을 초과하는 문제

### 프롬프트 10

테스트를 돌렸더니 앞 커밋에서 추가한 항목이 실패했다.

```
× 지연이 200~800ms 범위에서 매번 달라진다   5009ms
Error: Test timed out in 5000ms.
```

> 테스트 결과 문제있는데? (실패 로그 첨부)

### AI 출력 요지

원인은 측정 방식이었다. 8회를 **순차로** 재면 평균 500ms × 8 = 4초, 최악이면 800ms × 8 = 6.4초가 되어
vitest 기본 타임아웃 5초를 넘긴다.

`Promise.all`로 동시에 보내면 전체 소요가 합계가 아니라 **가장 느린 한 건**으로 정해지고, 표본 8개는 그대로 남는다.

### 리뷰 / 검증

- **무엇이 문제였나**: 기능 결함이 아니라 테스트 자체의 결함이다. 그런데 앞 커밋에서 이 테스트를 8회 샘플링으로 고치면서
  **"5회 연속 실행해 안정성을 확인했다"고 기록해 뒀다.** 그 근거가 틀렸다.
- **왜 5회 통과를 믿으면 안 됐나**: 실패율의 허용 오차는 표준편차를 계산해서 정했으면서,
  **소요 시간 분포는 따지지 않았다.** 평균 4초가 상한 5초에 붙어 있다는 걸 계산했다면 5회를 돌리기 전에 알았을 것이다.
  5회 통과는 확률 검증이지 구조 검증이 아니다.
- **어떻게 고쳤나**: `Promise.all`로 8회를 동시에 측정한다. 각 호출이 자기 시작·종료 시각을 재므로 표본의 의미는 같고,
  전체 소요만 최대 800ms로 줄어 타임아웃과 6배 여유가 생긴다.

  ```
  수정 전  4607ms (간헐적으로 5초 초과)
  수정 후   779ms
  전체 테스트 7.38초 → 2.77초
  ```
- **검증**: 10회 연속 실행해 전부 통과했다. 다만 이번에는 반복 횟수가 근거가 아니다.
  **전체 소요의 상한이 구조적으로 800ms로 정해진다는 점**이 근거다.
- **배운 것**: 실행 시간이 확률에 좌우되는 테스트는 반복 실행으로 안전을 확인할 수 없다.
  상한이 구조적으로 정해지도록 바꿔야 한다.

---

## [optimistic-update] 낙관적 업데이트와 실패 롤백

### 프롬프트 9

> M3을 만들자. 지문이 유일하게 "미충족 시 실격"으로 지목한 항목이라 여기서 시간을 아끼지 않는다.
>
> 이동을 누르면 화면을 먼저 바꾸고, API가 실패하면 원래대로 되돌린 뒤 사용자에게 알린다.
>
> 되돌릴 때 주의할 게 있다. 앞 커밋에서 **옮긴 카드를 대상 컬럼 맨 위로 보내도록** 했으니,
> 단계만 되돌리면 카드가 원래 컬럼에 돌아오되 엉뚱하게 맨 위에 남는다. 위치까지 되돌려야 한다.
>
> 전체 목록을 통째로 복사해서 스냅샷으로 쓰지 마라. 1,000건 배열을 이동할 때마다 복제하게 된다.
>
> 진행 중인 카드는 아직 확정되지 않았다는 걸 화면에 표시하고, 실패는 토스트로 알려라.

### AI 출력 요지

`pending: Record<string, { previousStage, previousIndex }>` 구조를 제안했다.
`move/start`에서 스냅샷을 남기고 화면을 먼저 바꾸며, `move/failure`에서 그 값으로 단계와 위치를 함께 되돌린다.

토스트는 `role="alert"`, 진행 중인 카드는 점선 테두리에 흐린 배경으로 구분한다.

### 리뷰 / 검증

- **채택 — 스냅샷을 카드 한 장 분량으로 제한**: 되돌리는 데 필요한 값은 그 카드의 단계와 위치뿐이다.
  전체 배열을 복제하면 이동마다 1,000개를 복사하게 되고, 연속 이동에서 메모리와 시간이 낭비된다.
- **직접 따져본 것 — 토스트가 연달아 뜨면 어떻게 되는가**: 실패율이 15%라 여러 카드를 빠르게 누르면
  같은 메시지가 연속으로 뜬다. 메시지 문자열만 상태로 두면 내용이 같을 때 React가 같은 것으로 보고
  자동 닫힘 타이머를 새로 시작하지 않는다. 앞선 타이머가 끝나면서 새 알림까지 같이 닫힌다.
  토스트에 증가하는 키를 붙여 매번 새 알림으로 처리되게 했다.
- **검증 — 실패를 강제해 롤백을 관찰**: 확률에 맡기면 재현이 안 된다. 개발 서버에서 `MOCK_CONFIG`를 조정해
  지연을 2초로 늘리고 실패율을 100%로 고정한 뒤, 낙관적 반영 시점과 롤백 시점을 나눠서 확인했다.

  ```
  대상: 강현희 (서류검토 200번째) | 이동 전: 437/226/86/81/170
  요청 중: 436/227/86/81/170 | 면접 맨 위: 강현희 | pending 카드: 1
  PASS  낙관: 즉시 대상 컬럼으로 이동 / 맨 위에 보임 / 진행 중 표시 / 버튼 비활성

  실패 후: 437/226/86/81/170
  PASS  롤백: 단계 원복 / 원래 위치(200번째) 복구 / 토스트 메시지 / role=alert / pending 해제

  성공 후: 436/227/86/81/170
  PASS  성공: 이동 확정 / 토스트 없음 / 대상 컬럼 맨 위 유지
        13/13 passed
  ```

  **실패 경로와 성공 경로를 모두 확인한 이유**는, 롤백만 맞고 성공 시 화면이 어긋나는 경우가 있어서다.
  낙관적으로 바꾼 값과 서버가 돌려준 값이 다를 수 있다.
- **화면 녹화로 남긴 것**: 자동 검증은 숫자만 맞춰볼 뿐이라, 사람이 봤을 때 실제로 롤백처럼 보이는지는 따로 확인했다.
  `docs/images/m3-rollback.gif`에 이동 → 낙관적 반영 → 실패 → 복구 → 토스트가 5초 안에 담겨 있다.
  컬럼 개수가 `437/226 → 436/227 → 437/226`으로 왕복한다.
  카드가 대상 컬럼 맨 위로 올라갔다가 원래 자리로 돌아오는 것이 눈에 보이는데,
  **앞 커밋에서 표시 위치를 고치지 않았다면 이 녹화 자체가 불가능했다.** 카드가 화면 밖에 있었을 것이다.
- **검증 중에 알게 된 것 — 리렌더가 120ms 안에 끝나지 않는다**: 처음에는 빌드 결과물로 검증하면서
  클릭 후 120ms에 낙관적 상태를 확인하려 했는데 실패했다. 카드 1,000장을 다시 그리는 데 그보다 오래 걸린다.
  기능 결함은 아니지만 가상 스크롤(S2)이 필요한 근거가 하나 더 늘었다.
- **남긴 한계**: `previousIndex`는 이동을 시작한 시점의 위치다. 서로 다른 카드를 거의 동시에 옮기면
  나중에 롤백되는 카드의 위치가 한 칸 어긋날 수 있다. 같은 카드를 연속으로 옮기는 경우와 함께 다음 커밋(S1)에서 다룬다.

---

## [search-filter] 이름 검색과 직무 필터

### 프롬프트 11

> M4를 만들자. 이름 검색 + 직무 필터다. 요구는 "200건 이상이어도 체감 지연 없이"인데 우리는 1,000건을 쓴다.
>
> 주의할 것: 병목은 필터링이 아니라 **글자를 칠 때마다 보드 전체가 다시 그려지는 것**이다.
> 1,000개 순회는 1ms도 안 걸리지만 카드 수백 장 재렌더는 오래 걸린다.
>
> debounce를 직접 만들지 말고 React 기능으로 풀어라. 대기 시간을 임의로 정하고 싶지 않다.
>
> 필터 결과가 0건일 때는 "등록된 지원자가 없습니다"가 아니라 다른 문구가 나와야 한다. 원인이 다르다.

### AI 출력 요지

`useDeferredValue`로 검색어의 사본을 만들어 목록 계산에만 쓰는 방식을 제안했다.
입력창은 즉시 갱신되고 보드 재계산은 한 박자 늦게 따라오며, 리액트가 급한 렌더를 먼저 처리한다.

`BoardFilters` 컴포넌트에 검색창·직무 선택·결과 개수를 두고, `BoardEmpty`에 `filtered` 여부를 넘겨 문구를 나눴다.

### 리뷰 / 검증

- **채택 — `useDeferredValue`**: 직접 만든 debounce는 대기 시간을 몇 ms로 할지 근거 없이 정해야 한다.
  빠른 기기에서는 불필요하게 기다리고 느린 기기에서는 여전히 끊긴다.
  `useDeferredValue`는 고정 시간 대신 렌더 우선순위로 처리해서 기기 성능에 맞춰진다.
- **직접 판단한 것 — 부분 일치인가 앞글자 일치인가**: 지문에 없어서 정해야 했다.
  앞글자만 허용하면 "민"으로 "김민준"을 찾을 수 없다. 채용 담당자가 성을 모르고 이름만 기억하는 경우가 흔하다고 보고 부분 일치로 갔다.
  가정이라 [DECISIONS.md](DECISIONS.md) D8에 남겼다.
- **확인한 것 — 필터와 단계 이동이 충돌하지 않는가**: 필터가 걸린 상태에서 카드를 옮기면
  화면 순서와 원본 순서가 어긋날 수 있다고 봤다. 코드를 따져보니 이동은 `byId`를 바꾸고 필터는 `order`를 걸러낼 뿐이라
  서로 간섭하지 않는다. 요구사항 순서를 M2·M3 뒤로 미룬 판단이 여기서 확인됐다.
- **검증 — 결과 수와 입력 반응성**: 필터 결과가 실제 카드 수와 맞는지, 타이핑이 밀리지 않는지 함께 봤다.

  ```
  PASS  초기 표시 "지원자 1000명"
  PASS  QA 필터 결과 일치   → 1000명 중 111명 / 카드 합계 111 / 직무는 QA만
  PASS  '김민' 검색 결과 일치 → 1000명 중 4명, 전부 '김민' 포함
  PASS  입력값 즉시 반영     → 두 글자 입력에 48ms
  PASS  빈 상태 문구         → "조건에 맞는 지원자가 없습니다"
  PASS  초기화 후 복구 (1000)
        9/9 passed
  ```

  **결과 개수 표시와 실제 카드 수를 따로 세어 비교한 이유**는, 개수만 맞고 화면에는 다른 카드가 남는 경우를 잡기 위해서다.

---

## [detail-panel] 지원자 상세 보기

### 프롬프트 12

> M5를 만들자. 카드를 클릭하면 지원자 상세가 열린다. 사이드 패널과 모달 중에 골라야 하는데,
> 이 화면의 주된 작업이 **상세를 보면서 단계를 옮기는 것**이라 보드가 가려지면 안 된다.
>
> 클릭을 `li`에 걸지 마라. 마우스로만 열리게 된다. 도전 요구사항에 "키보드만으로 상세 열람"이 있어서
> 나중에 `tabIndex`와 키 핸들러를 손으로 붙이게 된다.
>
> 패널에는 시드에 있는 항목을 다 보여주고, `Escape`로 닫히게 해라.

### AI 출력 요지

카드 본문을 `<button>`으로 감싸고 이동 버튼들은 그 바깥에 두는 구조를 제안했다.
초점 이동·키 입력·보조기기 역할이 전부 기본으로 따라온다.

패널은 `role="dialog"` + `aria-labelledby`, 열리면 닫기 버튼으로 초점 이동, `Escape` 키로 닫기.

### 리뷰 / 검증

- **채택 — 카드 본문을 버튼으로**: `li`에 `onClick`을 거는 것보다 코드가 조금 늘지만,
  `a11y-keyboard`(S5)에서 해야 할 일이 그만큼 줄어든다. 이동 버튼을 액션 버튼으로 택한 판단과 같은 이유다.
- **직접 판단한 것 — 초점을 패널 안에 가둘 것인가**: 모달이면 초점을 가두는 게 맞지만 이건 사이드 패널이다.
  보드를 가리지 않으므로 패널을 열어둔 채 카드를 조작할 수 있어야 한다. 초점 이동만 하고 가두지는 않았다.
  대신 `Escape`로 언제든 닫힌다.
- **확인한 것 — 패널이 살아 있는 데이터를 보는가**: 선택한 지원자를 `state.byId`에서 다시 읽도록 했다.
  열 때의 값을 복사해 두면 단계를 옮겨도 패널에는 예전 단계가 남는다.
  패널을 연 채로 이동 버튼을 눌러 단계 표시가 따라 바뀌는지 직접 확인했다.
- **검증**

  ```
  PASS  패널 제목 = 카드 이름 / role=dialog / aria-labelledby 연결
  PASS  상세 항목 7개 (현재 단계·직무·경력·지원일·지원 경로·이메일·연락처)
  PASS  열린 카드 표시 / aria-expanded=true / 초점이 닫기 버튼으로 이동
  PASS  패널을 연 채 이동 → 패널의 단계 표시 갱신
  PASS  Escape 로 닫힘 / 닫기 버튼으로 닫힘
        12/12 passed
  ```

### 프롬프트 13

만든 패널을 써 보니 문제가 있었다.

> 우측 패널에 상세가 보이는데, 패널이 너비를 가져가서 기존 카드들이 갑자기 좁아진다.
> 패널 등장은 지금처럼 두되 카드들 위로 뜨게 하고, 닫기는 X 같은 아이콘으로 하면 좋겠다.
> react-icons 나 lucide 같은 걸 쓰는 건 어떨까?
> 그리고 패널이 떠 있을 때 뒤쪽 화면은 반투명하게 어둡게 하고, 그 영역을 클릭하면 패널이 닫히게 해줘.

### AI 출력 요지

오버레이 방식으로 바꾸면 **카드의 이동 버튼을 누를 수 없게 된다**는 점을 먼저 지적했다.
사이드 패널을 고른 이유가 "상세를 보면서 이동"이었는데 배경을 덮으면 그 흐름이 막힌다.
이동 버튼을 패널 안으로 옮기면 오버레이를 깔면서도 흐름이 유지되고, 오히려 상세를 확인하고 그 자리에서 옮기는 편이 자연스럽다고 했다.

아이콘 라이브러리는 권하지 않았다. 필요한 아이콘이 X 하나뿐이라 인라인 SVG 열 줄이면 되고,
UI 라이브러리 없이 마크업을 직접 통제하기로 한 D1과도 어긋난다는 이유였다.

### 리뷰 / 검증

- **채택 — 이동 버튼을 패널로 이동**: 요청대로 오버레이만 씌웠다면 기능이 하나 막히는 걸 모르고 넘어갔을 것이다.
  UI 변경이 앞선 설계 결정의 전제를 깨뜨린다는 지적이 맞다고 봐서 받아들였다. [DECISIONS.md](DECISIONS.md) D9을 다시 썼다.
- **채택 — 아이콘 라이브러리 미사용**: X 하나를 위해 의존성을 더하면 스타일링 방침과 어긋난다.
  아이콘이 여러 개 필요해지는 시점에 다시 판단하기로 했다.
- **추가로 처리한 것 — 초점 가두기**: 배경을 덮는 형태가 되었으므로 `aria-modal="true"`를 붙이고 `Tab`을 패널 안에 가뒀다.
  가두지 않으면 보이지 않는 뒤쪽 카드로 초점이 새어 나간다. 비모달 패널이었을 때는 필요 없던 처리다.
- **수정 — 초점이 `body`로 떨어지는 경우**: 초점 가두기를 넣고 검증했더니 실패했다.

  ```
  FAIL  Tab 이 패널 안에 갇힘   실제: false
  ```

  원인은 **패널 안의 이동 버튼을 누르면 그 버튼이 `disabled`가 되면서 초점이 `body`로 떨어지는 것**이었다.
  초점이 패널 밖에 있으니 "첫 요소인가 / 마지막 요소인가" 판정이 둘 다 빗나가 그대로 새어 나갔다.
  초점이 패널 밖에 있으면 다시 안으로 끌어오는 분기를 추가했다.
- **검증 — 요청한 항목을 하나씩**

  ```
  PASS  보드 너비 그대로 (패널을 열어도 밀리지 않음)
  PASS  오버레이 존재 / 반투명 rgba(9, 30, 66, 0.45)
  PASS  패널이 보드 위에 렌더
  PASS  닫기 버튼 aria-label="상세 닫기" / 아이콘 SVG 는 aria-hidden
  PASS  aria-modal="true"
  PASS  패널 안 이동 버튼으로 단계 변경 가능
  PASS  Tab 이 패널 안에 갇힘
  PASS  오버레이 클릭으로 닫힘 / X 버튼으로 닫힘
        13/13 passed
  ```

---

## [test] 롤백 로직 테스트

### 프롬프트 14

> S4 (`test(optimistic-update)`) 진행할게. 핵심 로직인 낙관적 업데이트 롤백을 테스트로 고정한다.
>
> 브라우저로 이미 확인했지만 그건 한 번 돌려본 것이고, 앞으로 경쟁 상태 처리를 넣으면서
> 리듀서를 고치게 된다. 그때 롤백이 깨졌는지 알려줄 게 필요하다.
>
> 리듀서는 순수 함수니까 렌더링 없이 액션만 넣고 결과를 보면 된다. 컴포넌트를 띄우지 마라.
>
> 이것도 같이 검증해라. `getStageMoves`가 단계별로 맞는 경로를 주는지. 특히 불합격 분기.

### AI 출력 요지

`applicantsReducer.test.ts`(12건)와 `types.test.ts`(4건)로 나눴다.
리듀서는 `fetch/success`로 만든 5명짜리 상태에 액션을 적용해 결과만 비교한다.

롤백은 세 가지를 따로 확인한다 — 단계 복구, 표시 위치 복구, 그리고 이동 전 상태와의 전체 일치.

### 리뷰 / 검증

- **채택 — 컴포넌트를 띄우지 않는 방식**: `@testing-library/react`로 렌더해서 클릭을 흉내 낼 수도 있지만,
  검증 대상은 상태 전이지 화면이 아니다. 리듀서에 액션을 넣고 결과를 비교하는 편이 빠르고 실패 원인도 명확하다.
  실제로 리듀서 테스트 12건이 전부 0ms대에 끝나 전체 실행 시간이 거의 늘지 않았다.
- **추가한 것 — "롤백은 이동 전 상태와 완전히 같아야 한다"**: AI 초안은 단계와 위치를 따로 확인했다.
  그것만으로는 **내가 확인하기로 한 것만 맞는지** 보는 것이라, 생각하지 못한 값이 남아도 통과한다.
  `byId`와 `order` 전체를 이동 전 상태와 통째로 비교하는 테스트를 넣었다.
- **추가한 것 — 스냅샷이 없는 실패**: `move/start` 없이 `move/failure`가 오는 경우다.
  이론상 생기지 않지만 리듀서가 방어하고 있는 분기라 한 번은 밟아봐야 한다.
  같은 이유로 없는 지원자를 옮기는 경우도 넣었다. 둘 다 상태 객체가 그대로여야 한다(`toBe`로 참조까지 비교).
- **추가한 것 — 알림 키 증가**: `optimistic-update`(M3)에서 토스트에 키를 붙인 이유를 테스트로 고정했다.
  키가 없으면 같은 메시지가 연달아 뜰 때 자동 닫힘 타이머가 갱신되지 않는다.
  코드만 보고는 왜 키가 있는지 알기 어려워 테스트가 설명 역할도 한다.
- **불합격 분기 검증**: `getStageMoves("불합격")`이 `{ prev: "서류검토", next: null, canReject: false }`를 주는지 확인했다.
  이 값이 `stage-move`(M2)에서 내린 가정을 코드로 고정한 것이다.
- **검증 결과**

  ```
  ✓ 조회 (2)         id 맵/순서 분리, 재조회 시 진행 중 기록 초기화
  ✓ 낙관적 이동 (3)   응답 전 단계 변경·맨 앞 이동, 스냅샷 저장, 없는 id 무시
  ✓ 성공 (1)          서버 값 반영, 스냅샷 삭제, 맨 앞 유지
  ✓ 실패 롤백 (6)     단계 복구, 위치 복구, 전체 상태 일치, 메시지, 키 증가, 스냅샷 없음
  ✓ 단계 경로 (4)     첫 단계·중간·최종합격·불합격 분기

  Test Files  4 passed (4)
       Tests  33 passed (33)
  ```

  `test(mock-api)` 시점의 17건에서 33건이 되었고, 실행 시간은 거의 그대로다.

---

## [race-condition] 같은 카드를 연속으로 옮길 때의 경쟁 상태

### 프롬프트 15

> S1 (`fix(race-condition)`) 진행할게. `optimistic-update`(M3)에서 남겨둔 한계를 정리한다.
>
> 지금 구조의 구멍이 두 개로 보인다.
> 1. 같은 카드를 연타하면 요청이 겹쳐서 서버 도착 순서가 클릭 순서와 달라질 수 있다
> 2. 스냅샷을 매번 덮어써서, 두 번째 클릭이 첫 번째의 낙관적 값을 "이전 상태"로 기록한다.
>    실패하면 서버가 확정한 적 없는 값으로 되돌아간다
>
> "요청에 번호를 붙이고 늦게 온 응답은 버린다"는 방법은 쓰지 마라.
> mock 서버는 도착 순서대로 덮어쓰니까 응답을 무시해도 서버에는 이미 저장된 뒤다. 화면만 최신으로 보이고 서버는 다른 값을 들게 된다.
>
> 그리고 롤백 위치를 인덱스로 기억하는 것도 고쳐라. 다른 카드가 앞으로 이동하면 한 칸씩 밀린다.
>
> 화면은 줄 세우지 마라. 낙관적 업데이트의 이점이 사라진다.

### AI 출력 요지

카드별 프라미스 사슬(`createRequestQueue`)로 요청만 직렬화하고 화면은 즉시 갱신한다.
스냅샷에 `inFlight` 카운터를 두어 첫 요청 때만 기록하고, 마지막 응답일 때만 확정하거나 되돌린다.
위치는 인덱스 대신 **바로 앞 카드의 id**를 기준점으로 저장한다.

### 리뷰 / 검증

- **채택 — 직렬화**: 응답 순서 비교 방식은 화면과 서버가 어긋날 수 있다는 지적이 맞았다.
  `previous.then(task, task)`로 앞 요청이 실패해도 뒤 요청을 이어 보낸다. 사용자가 누른 의도는 모두 반영돼야 한다.
- **직접 판단한 것 — 중간 실패를 알릴 것인가**: 세 번 눌렀는데 두 번째만 실패하면 토스트를 띄울지 고민했다.
  뒤 요청이 성공하면 서버와 화면이 모두 사용자의 마지막 의도와 맞는다. 알릴 문제가 없어서 마지막 응답만 알리기로 했다.
- **확인한 것 — 큐가 렌더마다 새로 만들어지지 않는가**: `createRequestQueue()`를 컴포넌트 본문에서 호출하면
  렌더될 때마다 줄이 끊긴다. `useRef`에 담아 한 번만 만들도록 했다.
- **검증 — 리듀서**: 경쟁 상태 6건을 추가했다. 특히 다음 두 가지가 이번 수정의 핵심이다.

  ```
  ✓ 같은 카드를 연속으로 누르면 스냅샷을 덮지 않는다
    → 덮였다면 확정된 적 없는 '면접'으로 되돌아간다
  ✓ 마지막 응답이 실패면 처음 확정 상태로 되돌린다
    → 중간에 성공한 '면접'이 아니라 누르기 전의 '서류검토'
  ✓ 앞선 요청이 실패해도 뒤 요청이 남아 있으면 되돌리지 않는다
  ✓ 마지막 응답이 성공이면 그 값으로 확정된다
  ✓ 다른 카드가 먼저 이동해도 원래 자리로 되돌아간다
  ✓ 두 카드를 동시에 옮겨도 서로의 롤백에 간섭하지 않는다

  Tests  40 passed (40)
  ```
- **검증 — 브라우저**: 지연을 400~900ms로 늘리고 같은 카드를 60ms 간격으로 연타한 뒤,
  **화면에 보이는 단계와 localStorage 에 저장된 단계를 직접 비교**했다.

  ```
  연타 후 분포: 434/229/86/81/170
  대상: 임태연 | 서버 저장: 면접 | 화면 표시: 면접
  PASS  화면과 서버가 일치 / 진행 중 표시 남지 않음 / 카드 총수 유지
  ```

  화면만 확인하면 "그럴듯해 보이는 화면"에 속는다. 경쟁 상태의 증상은 **화면과 저장소가 어긋나는 것**이라 둘을 따로 읽어 비교해야 한다.
- **남은 한계**: 이동 버튼은 요청 중에 `disabled` 되므로 실제 사용자가 같은 카드로 요청을 겹치게 만들기는 어렵다.
  직렬화는 그 방어가 뚫렸을 때를 위한 것이다. 버튼 비활성화만 믿으면 상세 패널과 카드에서 동시에 누르는 경로가 열려 있다.

---

## [a11y-keyboard] 키보드만으로 단계 이동과 상세 열람

### 프롬프트 16

> S5 (`feat(a11y-keyboard)`) 진행할게.
>
> 조작 자체는 이미 될 거다. 이동은 버튼이고 카드 본문도 버튼으로 만들어 뒀다.
> 그러니 "된다"를 확인만 하지 말고 **실제로 키보드만으로 연속 작업이 되는지** 먼저 확인해라.
> 안 되는 게 있으면 그걸 고치는 게 이번 작업이다.

### AI 출력 요지

확인해 보니 **단계를 옮기면 초점이 `body`로 떨어졌다.**
카드가 다른 컬럼으로 옮겨가며 DOM 에서 제거됐다 다시 생성되기 때문이다.

이동을 요청한 카드에 초점 요청을 남기고 다시 그려질 때 스스로 초점을 되찾게 하는 방식을 제안했다.
요청에는 순번을 붙여, 이동할 때만 순번이 올라가고 그 렌더에서만 초점을 가져가게 했다.

### 리뷰 / 검증

- **먼저 확인한 것 — 정말 되는가**: "버튼이니까 키보드로 된다"는 가정을 검증부터 했다.

  ```
  이동 전 카드: 임태연 | 이동 후 초점: {"tag":"BODY","inCard":false}
  FAIL  이동 후에도 초점이 카드 안에 남는가
  ```

  조작은 되지만 **연속 작업이 안 된다.** 이동 한 번 뒤에 `Tab`을 처음부터 수백 번 눌러야 다음 카드에 닿는다.
  요구사항을 "키보드로 누를 수 있는가"로 읽으면 통과지만, "키보드만으로 쓸 수 있는가"로 읽으면 통과가 아니다.
- **채택 — 초점 요청에 순번 붙이기**: 대상 id 만 두면 상관없는 렌더에서도 초점을 빼앗는다는 지적이 맞았다.
  검색어를 칠 때마다 보드가 다시 그려지는데, 그때마다 마지막으로 옮긴 카드가 초점을 가져가면 입력창에서 글자를 칠 수 없다.
  실제로 이 경우를 검증 항목에 넣었다.
- **검증 — 키보드만으로 전체 흐름**

  ```
  이동한 카드: 임태연 | 이동 후 초점이 머문 카드: 임태연
  PASS  이동 후 초점이 같은 카드에 남는다
  PASS  타이핑 중 초점 유지        (검색어를 쳐도 카드가 초점을 뺏지 않음)
  PASS  Enter 로 상세 열림 / 초점이 패널로 / Escape 로 닫힘
  PASS  초점 테두리 보임           outline 2px solid
        7/7 passed
  ```

  **초점 표시가 실제로 그려지는지도 확인했다.** `:focus-visible` 규칙을 써 두고도 다른 스타일에 덮여
  아무 표시가 안 나오는 경우가 있어서, 계산된 `outline` 값을 직접 읽었다.
### 프롬프트 17

초점 복구를 넣고 나서, 상세 패널과 겹치는 경우를 따로 묻었다.

> 상세 패널을 상단 레이어로 옮겼고 초점이 패널로 가 있는 상태 아닌가?
> 그러면 패널 상태에서 좌우로 이동이 되어야 하거나, 패널 상태를 바꾸거나 둘 중 하나를 골라야 하는 거 아닌지 확인해줘.

### AI 출력 요지

패널 안에 이미 이동 버튼이 있어 `Tab` + `Enter` 로 단계를 옮길 수 있으므로 별도 단축키는 필요 없다고 답했다.
다만 확인해 보니 **초점 처리에 문제가 세 군데 있었다.**

### 리뷰 / 검증

- **버그 1 — 패널에서 이동하면 초점이 뒤쪽 카드로 샌다**: 보드용 이동 핸들러를 패널에도 그대로 넘겨서,
  패널에서 눌러도 카드가 초점을 가져갔다.

  ```
  패널에서 이동 후 초점: {"inPanel":false,"inCard":true,"text":"임태연QA지원일 2026-"}
  ```

  **화면에는 패널이 떠 있는데 초점은 오버레이 뒤의 보이지 않는 카드에 있는** 상태다.
  패널에서 이동할 때는 초점을 패널 안에 두도록 분리했다.
- **버그 2 — 패널을 닫으면 초점이 `body` 로 떨어진다**: 모달의 기본 규칙인 "닫으면 열었던 자리로"가 빠져 있었다.
  처음에는 열 때의 DOM 요소를 기억했다가 되돌리게 했는데, 그래도 실패했다.
  **패널에서 단계를 옮기면 그 카드가 다른 컬럼으로 이동하면서 열었던 버튼 자체가 사라진다.**
  요소가 아니라 카드 id 로 되돌리도록 바꿨다.
- **버그 3 — 지난 초점 요청이 남아 재사용된다**: 위 둘을 고치고 전체 흐름을 이어서 돌리자 다시 실패했다.
  카드는 컬럼을 옮길 때마다 다시 마운트되는데, 그때 **이미 처리된 옛 요청으로 초점을 또 가져갔다.**
  단독 검증에서는 드러나지 않고 "보드 이동 → 상세 열기 → 패널에서 이동" 순서로 이어 붙였을 때만 나왔다.
  카드가 초점을 받으면 요청을 비우도록 고쳤다.
- **검증 — 흐름을 끊지 않고 이어서**

  ```
  PASS  보드 이동 후 같은 카드에 초점
  PASS  Enter 로 상세 열림, 초점 패널로
  PASS  패널 이동 후 초점 패널 유지
  PASS  닫으면 열었던 카드로 복귀
  PASS  타이핑 중 초점 유지
        6/6 passed
  ```

  **동작을 하나씩 따로 확인했을 때는 세 개 중 두 개가 통과했다.** 실제로 쓰는 순서대로 이어 붙이자 남은 버그가 드러났다.
  초점은 앞선 조작의 결과를 이어받는 값이라, 단독 검증만으로는 충분하지 않다.
- **남긴 한계 — 1,000장에서의 `Tab` 이동**: 먼 컬럼의 카드까지 `Tab`으로만 가려면 수백 번을 눌러야 한다.
  검색으로 좁힌 뒤 조작하는 우회로가 있지만 근본 해결은 아니다.
  컬럼 안에서 방향키로 이동하고 `Tab`은 컬럼 단위로 건너뛰는 방식(roving tabindex)이 필요한데 시간 안에 넣지 못했다.
  [DECISIONS.md](DECISIONS.md) 미완료 항목에 남겼다.

---

## [undo] 방금 한 이동 되돌리기

### 프롬프트 18

> S3 (`feat(undo)`) 진행할게. 방금 한 이동을 취소한다.
>
> 주의할 게 있다. **되돌리기는 실패 롤백과 다르다.** 롤백은 서버가 거절해서 화면을 원래대로 돌리는 거고,
> 되돌리기는 **이미 서버까지 반영된 이동을 반대 방향의 새 요청으로 뒤집는** 것이다.
> 화면에서만 처리하면 서버에 남은 값과 어긋난다.
>
> 그리고 되돌리기 요청 자체도 15% 확률로 실패한다. 특별 취급하지 말고 평범한 이동으로 보내라.
>
> 무엇이 되돌려지는지 화면에 보여라. 카드가 1,000장이라 "되돌리기"만 있으면 무엇이 취소되는지 알 수 없다.

### AI 출력 요지

`move/success` 시점에만 `lastMove`를 기록하고, `undo`는 그 출발 단계를 목표로 하는 일반 이동 요청을 보낸다.
낙관적 반영·실패 롤백·요청 직렬화가 그대로 적용된다.

되돌린 직후 `lastMove`를 비우고, 되돌리기가 성공하면 그것이 새 되돌리기 대상이 된다.
`UndoBar`에 `이름 · 출발 → 도착`을 띄우고 `role="status"`로 알린다.

### 리뷰 / 검증

- **채택 — 되돌리기를 특별 취급하지 않는 것**: 전용 경로를 만들면 낙관적 반영과 롤백, 직렬화를 다시 구현해야 한다.
  일반 이동으로 보내면 이미 검증된 경로를 그대로 탄다. 되돌리기 요청이 실패하면 롤백도 알아서 동작한다.
- **직접 판단한 것 — `move/start`가 아니라 `move/success`에서 기록**: 화면만 바뀐 상태를 기억하면
  서버에 존재하지 않는 변경을 취소하려 든다. 특히 실패한 이동을 되돌리려 하면 엉뚱한 단계로 보내게 된다.
  테스트로 고정했다 — "실패한 이동은 되돌리기 대상이 아니다".
- **수정 — 렌더 중에 `ref`를 건드리고 있었다**: `undo`가 최신 상태를 보게 하려고 `ref.current`를 렌더 본문에서 대입했는데
  린터가 잡았다.

  ```
  warning react(refs): Cannot access refs during render
  ```

  동작은 했지만 렌더 도중 값을 바꾸면 리액트가 렌더를 버리고 다시 실행할 때 어긋날 수 있다.
  `useEffect` 안에서 맞추도록 고쳤다.
### 프롬프트 21

되돌리기를 만들고 나서, 되돌리기 자체가 실패하는 경우를 물었다.

> 되돌리기로 되돌리는데 "단계 이동을 저장하지 못했습니다"가 뜰 수도 있는 거야?

### AI 출력 요지

그렇다고 답했다. 되돌리기도 일반 이동 요청이라 15% 확률로 실패한다.
그런데 확인해 보니 **그 경우에 문제가 두 가지 있었다.**

### 리뷰 / 검증

- **버그 — 실패하면 다시 시도할 수 없다**: `undo`가 요청을 보내기 전에 `lastMove`를 비우고 있었다.
  되돌리기가 실패하면 카드는 제자리로 롤백되고 토스트는 뜨는데, **되돌리기 바가 사라져 재시도 경로가 없다.**
  요청 전에 비우지 않도록 바꿨다. 성공하면 `move/success`가 새 대상으로 덮어쓰고,
  실패하면 `move/failure`가 `lastMove`를 건드리지 않아 바가 그대로 남는다.
- **수정 — 실패 메시지가 상황과 어긋난다**: 되돌리기를 눌렀는데 `단계 이동을 저장하지 못했습니다`가 뜬다.
  기술적으로는 맞지만 사용자는 자기가 무엇을 눌렀는지 헷갈린다.
  `move`에 실패 메시지를 선택적으로 넘길 수 있게 하고 되돌리기에서는 `되돌리기를 저장하지 못했습니다.`를 쓴다.
- **검증 — 실패를 강제하고 재시도까지**

  ```
  이동:         437/226 → 436/227   바: "임태연 · 서류검토 → 면접"
  되돌리기 실패: 카드는 이동한 자리에 남음
               토스트 "되돌리기를 저장하지 못했습니다."
               되돌리기 바 그대로 유지 (내용도 동일)
  재시도 성공:   437/226 으로 복귀
        5/5 passed
  ```

  리듀서 테스트도 추가했다 — "이동이 실패해도 되돌리기 대상은 남는다" (45 → 46).
- **배운 것**: 되돌리기를 "일반 이동으로 처리한다"고 정하면 **실패 경로도 함께 따라온다.**
  기능을 재사용하면 그 기능의 실패 방식까지 재사용된다는 걸 놓쳤다.

- **검증 — 화면과 서버를 따로 확인**: 되돌리기의 핵심이 "서버까지 되돌아가는가"라 둘을 따로 읽었다.

  ```
  임태연: 437/226/86/81/170 → 436/227/86/81/170
  PASS  이동 후 되돌리기 바 표시 "임태연 · 서류검토 → 면접"
  PASS  서버에 면접으로 저장
  되돌린 뒤: 437/226/86/81/170
  PASS  되돌리면 분포 원복 / 서버도 되돌아감
  PASS  되돌리기는 한 번만 (바가 "임태연 · 면접 → 서류검토" 로 바뀜)
  PASS  초점이 되돌린 카드로
        7/7 passed
  ```

  리듀서 테스트 5건도 추가했다 (40 → 45).

---

## [virtualization] 1,000건 가상 스크롤

### 프롬프트 19

> S2 (`feat(virtualization)`) 진행할게. `board-layout`(M1)에서 재둔 기준선이 있다.
> DOM 노드 6,034개, 스크롤 프레임 중앙값 8.3ms, **최대 311.9ms**.
>
> 문제는 평균이 아니라 간헐적 스파이크다. 그걸 없애는 게 목표다.
>
> 라이브러리는 쓰지 마라. 컬럼마다 목록이 따로 스크롤되고 카드 높이가 전부 같아서
> 측정 없이 나눗셈으로 구간을 구할 수 있다. 그 조건이면 의존성을 더할 이유가 없다.
>
> 끝나고 나서 **기존 기능이 깨지지 않았는지** 같이 확인해라. 렌더링 구조를 바꾸는 작업이다.

### AI 출력 요지

`useVisibleRange` 훅으로 `scrollTop`에서 그릴 구간을 계산하고, 보이지 않는 앞뒤 구간은 빈 `li` 두 개가 높이만 차지하게 했다.
앞뒤로 6장씩 더 그려(overscan) 빠른 스크롤에서 빈 칸이 보이지 않게 하고,
스크롤 이벤트는 `requestAnimationFrame` 으로 묶어 프레임당 한 번만 계산한다.

### 리뷰 / 검증

- **채택 — 직접 구현**: 카드 높이가 고정이라 계산이 나눗셈 두 줄이다.
  `react-window` 같은 라이브러리는 가변 높이·수평 스크롤·그리드까지 다루느라 설정할 것이 많다.
  필요하지 않은 기능을 위해 의존성을 더하지 않는다는 판단은 아이콘 라이브러리를 쓰지 않은 것과 같다.
- **직접 처리한 것 — 카드 높이 고정**: 가상 스크롤이 높이로 위치를 계산하므로 카드가 내용에 따라 늘어나면 어긋난다.
  `CARD_SLOT_HEIGHT` 상수와 CSS 의 `height` 를 함께 고정하고, 둘이 짝이라는 주석을 양쪽에 남겼다.
  한쪽만 바꾸면 카드가 겹치거나 벌어진다.
- **확인한 것 — 기존 기능이 깨지지 않았는가**: 렌더링 구조를 바꾸는 작업이라 회귀를 따로 확인했다.
  상세 패널 열기, 검색, 컬럼 개수 표시를 모두 다시 돌렸다. 멀리 스크롤한 뒤에도 카드가 그려지는지 확인했다.
### 프롬프트 20

가상 스크롤을 넣고 화면을 보니 카드 안쪽이 답답했다.

> 카드에서 `← 버튼 버튼 →` 이 하단의 공간이 좁다. 카드 내용이 패딩 영역까지 침범해서
> `card__actions` 하단이 카드 테두리와 거의 맞닿아 있다. 스타일 확인해줘.
> 그리고 되돌리기가 계속 떠 있는데 닫기(X) 같은 게 있으면 어떨까? 이건 그냥 의견이야.

### AI 출력 요지

가상 스크롤을 넣으면서 카드 높이를 120px 로 고정했는데 내용이 118px 이라 아래 패딩이 눌린 것이라고 답했다.
높이를 124px 로 늘리고 본문이 남는 높이를 차지하도록 바꾸면 위아래 여백이 같아진다.
`CARD_SLOT_HEIGHT` 도 함께 올려야 카드가 겹치지 않는다.

### 리뷰 / 검증

- **원인 확인 — 눈대중 대신 측정**: "좁아 보인다"를 감으로 고치지 않고 실제 값을 읽었다.

  ```
  {"카드높이":120,"패딩":"10px","내용높이":118,"액션아래여백":5}
  ```

  패딩은 10px 인데 아래 여백이 5px 이다. 내용이 카드 높이에 꽉 차서 패딩을 절반 먹었다.
  **가상 스크롤을 넣으며 높이를 고정한 부작용**이지 원래 있던 문제가 아니다.
- **고친 방식**: 높이만 늘리면 내용이 위로 몰린다. 본문에 `flex: 1`, 액션에 `margin-top: auto` 를 줘서
  본문이 남는 높이를 차지하고 액션은 아래에 붙되 패딩만큼 여백을 남기게 했다.
- **함께 확인한 것 — 카드가 겹치지 않는가**: 높이를 바꾸면 `CARD_SLOT_HEIGHT` 와 어긋나 카드가 겹치거나 벌어진다.
  실제 카드 사이 거리를 재서 슬롯 높이와 같은지 확인했다.

  ```
  {"카드높이":124,"본문위여백":11,"액션아래여백":11,"넘침":false}
  카드 간 거리: 132, 132      (CARD_SLOT_HEIGHT 와 일치)
  ```

  위아래 여백이 11px 로 같아졌다. 패딩 10px 에 테두리 1px 이 더해진 값이다.
- **분리한 것 — 되돌리기 닫기 버튼**: 같은 메시지에 담겨 있었지만 다른 기능이라 `undo` 쪽에서 처리했다.
  가상 스크롤 커밋에 되돌리기 변경을 섞으면 커밋 하나에 기능 둘이 들어간다. 내용은 [undo] 섹션에 있다.

- **검증 — 기준선과 같은 방법으로 측정**

  ```
                이전        이후
  DOM 카드      1,000장  →  56장
  DOM 노드      6,034    →  637      (-89%)
  프레임 중앙값  8.3ms    →  8.3ms
  프레임 최대   311.9ms  →  9.4ms    (-97%)

  PASS  DOM 카드가 크게 줄었다 / 컬럼 개수는 그대로 (1000)
  PASS  멀리 스크롤해도 카드가 보인다 / 스크롤 높이 55,936px
  PASS  상세 패널 동작 / 검색 동작 / 콘솔 에러 없음
        7/7 passed
  ```

  **중앙값은 그대로고 최대값이 사라졌다.** 원래 문제가 평균이 아니라 간헐적 스파이크였기 때문이다.
  중앙값만 봤다면 "개선 없음"으로 잘못 읽었을 것이다.
