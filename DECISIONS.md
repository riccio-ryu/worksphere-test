# 설계 결정 기록

결정이 실제로 필요해지는 시점에 한 항목씩 추가한다.
요구사항이 모호한 지점은 스스로 가정하고 그 가정도 함께 남긴다.

## D1. 스택 — Vite + React + TypeScript

지문이 스택을 자유로 열어두었고 Next.js도 후보로 언급돼 있어 둘을 비교했다.

Next.js를 택하지 않은 이유는 이 과제에 서버가 없기 때문이다.
mock API를 자체 구현해야 하는데, 지연·실패·영속을 전부 브라우저 안에서 흉내 내는 구조라 서버에서 미리 그릴 화면이 없다.
App Router를 쓰면 서버 컴포넌트 경계와 캐시 무효화를 신경 써야 하는데, 여기서는 얻는 것 없이 다룰 개념만 늘어난다.

평가 대상이 보드의 상태 관리와 실패 처리라고 보고, 그 외 요소를 줄이는 쪽을 택했다.
Vite는 개발 서버 기동과 HMR이 빨라 이동·롤백처럼 손으로 반복 확인해야 하는 기능을 만들 때 유리하다는 점도 고려했다.

스타일링은 외부 UI 라이브러리 없이 순수 CSS로 간다.
키보드 접근성이 도전 요구사항에 있어 마크업과 초점 처리를 직접 통제하는 편이 낫다고 봤다.
컴포넌트 라이브러리를 쓰면 초점 관리가 라이브러리 안으로 숨는다.

### 템플릿에서 걷어낸 것

`npm create vite`가 만든 데모 코드는 전부 지웠다. 지문이 "미사용 코드 방치"를 감점 신호로 지목하고 있다.

- `src/App.css`, `src/assets/*` (hero 이미지, React·Vite 로고) — 데모 전용
- `public/favicon.svg` — Vite 로고였다. 제출물의 파비콘으로 남기기에 부적절해서, 단계별 컬럼을 형상화한 SVG로 직접 그려 교체했다
- `public/icons.svg` — 데모 App에서만 참조하던 아이콘 스프라이트
- `src/index.css` 111줄 — 데모 스타일이라 색상 토큰과 기본 리셋만 남기고 새로 썼다

## D2. mock API — 직접 구현 + localStorage 영속

MSW(Mock Service Worker) 같은 라이브러리를 쓰지 않았다. 지문이 자체 구현을 명시했고,
요구도 지연 200~800ms · 실패 약 15%로 구체적이라 직접 만드는 편이 짧다.

새로고침 후에도 이동 결과가 유지돼야 하므로 상태를 localStorage에 둔다.
정리하면 이 mock API는 **"느리고 가끔 실패하는 localStorage 래퍼"** 다.
서버 역할이므로 낙관적 갱신은 하지 않는다. 실패하면 저장소를 건드리지 않고 그대로 던지고, 롤백은 호출하는 쪽 책임으로 남긴다.

### 가정 — 실패율 15%는 쓰기에만 적용한다

지문은 "실패(약 15%)를 시뮬레이션할 것"이라고만 하고 어느 요청에 적용할지는 말하지 않는다.
**조회에도 15%를 걸면 보드를 여는 것 자체가 도박이 된다.** 평가자가 새로고침을 몇 번 해야 화면을 볼 수 있고,
그마저도 실패하면 기능을 확인할 방법이 없다.

실패율은 단계 이동 같은 쓰기 요청에만 적용했다. 낙관적 업데이트와 롤백이 검증 대상인 지점도 쓰기 쪽이다.
조회 실패율은 `MOCK_CONFIG.fetchFailureRate`로 분리해 기본값 0으로 두되, 에러 상태 UI를 확인할 때 켤 수 있게 했다.

### 시드를 고정한 이유

`Math.random`으로 생성하면 실행할 때마다 데이터가 달라져 "1,000건 중 필터 결과가 몇 건" 같은 단언을 테스트에 쓸 수 없다.
선형 합동 생성기(LCG)에 고정 시드를 넣어 같은 데이터가 재현되게 했다.

지원일도 마찬가지 이유로 모듈 로드 시점의 시각을 한 번 고정해서 쓴다. 같은 실행 안에서 두 번 생성해도 결과가 같다.

### 저장소 키에 버전을 붙인 이유

`worksphere:applicants:v1`. 시드 로직을 바꿨는데 브라우저에 예전 데이터가 남아 있으면
새 코드로 동작을 확인할 수 없다. 키를 올리면 예전 데이터와 섞이지 않는다.

## D3. 상태 저장 구조 — 배열이 아니라 id 맵 + 순서 배열

지원자 목록을 `Applicant[]` 하나로 들고 있지 않고 두 조각으로 나눴다.

```ts
byId:  Record<string, Applicant>   // 카드 한 장을 O(1)로 찾고 갱신
order: string[]                    // 표시 순서
```

배열로 두면 카드 한 장을 옮길 때마다 1,000개를 순회해 새 배열을 만들어야 한다.
이 과제는 **단건 갱신이 반복되는 화면**이다. 단계 이동이 핵심 동작이고, 도전 요구사항에는
같은 카드를 빠르게 연속으로 옮기는 경우까지 들어 있다. 연타할 때마다 1,000개를 복사하면 부담이 쌓인다.

순서를 `order` 배열로 분리한 이유는 검색·필터 때문이다.
이름 검색과 직무 필터는 **보여줄 id 목록만 줄이면 되고 원본 데이터는 건드릴 필요가 없다.**

낙관적 업데이트에서도 이 구조가 유리하다. 화면에 보여줄 값과 서버가 확정한 값을 따로 들고 있어야 하는데,
id를 키로 쓰면 두 맵을 나란히 두고 같은 키로 조회할 수 있다.

### `useReducer`를 고른 이유

`useState`를 여러 개 두면 "이동 중인지", "확정된 단계가 무엇인지", "화면에 보이는 단계가 무엇인지"가 흩어진다.
이 셋은 **함께 바뀌어야 하는 값**이라 한 번의 상태 전이로 묶는 편이 안전하다.
지금은 조회 액션 세 개뿐이지만, 이동·성공·실패 액션이 붙으면 전이 표가 그대로 드러난다.

## D4. 시연용 조작 패널을 화면에 남긴 이유

보드 위에 노란색 "시연용" 패널을 뒀다. 조회 실패 강제 토글, 데이터 비우기, 초기 데이터로 되돌리기 세 가지다.
제품 기능이 아니라 mock 환경을 조작하는 장치다.

**에러 상태와 빈 상태는 평소에 재현되지 않는다.** 조회 실패율은 기본 0이고(D2), 시드는 항상 1,000건을 만든다.
이 패널이 없으면 세 상태를 모두 구현해도 두 개는 확인할 방법이 없다.

실제 제품이라면 빼야 할 코드다. 다만 이 과제는 mock 환경 자체가 요구사항이고,
구현한 것을 확인할 수 있게 만드는 편이 낫다고 판단했다. 제품 기능과 구분되도록 배경색과 "시연용" 표시를 붙였다.

## D5. 로딩 중에 빈 화면 대신 컬럼 골격을 보여주는 이유

mock API 지연이 200~800ms라 로딩 구간이 눈에 띈다.
`불러오는 중` 문구만 두면 데이터가 도착하는 순간 화면이 통째로 바뀌면서 튄다.

컬럼 다섯 개의 골격을 먼저 그리고 카드 자리만 회색 블록으로 채웠다. 데이터가 오면 블록이 카드로 바뀔 뿐 레이아웃은 그대로다.
골격은 읽을 내용이 없으므로 `aria-hidden`으로 보조기기에서 감추고, 상태는 `aria-live` 영역의 문구로 알린다.
움직임에 민감한 사용자를 위해 `prefers-reduced-motion`에서는 반짝임 애니메이션을 끈다.

## D6. 단계 이동 — 드래그앤드롭이 아니라 액션 버튼

지문은 둘 중 택일이지만, 도전 요구사항에 *"키보드만으로 단계 이동이 가능해야 하며, 드래그앤드롭을 택했다면 키보드 대체 경로 필수"* 가 붙어 있다.

드래그앤드롭을 고르면 사실상 조작 경로를 두 벌 만들어야 한다. **버튼은 그 자체가 키보드 경로다.**
`Tab`으로 초점이 가고 `Enter`·`Space`로 눌린다. 접근성을 위해 따로 만들 것이 없다.

여기서 아낀 시간을 낙관적 업데이트 롤백과 경쟁 상태 처리에 쓰기로 했다.
감점/실격 신호에 직접 언급된 항목이 롤백 쪽이라 배점이 더 무겁다고 봤다.

> **가정**: 평가 대상은 "이동이 안정적으로 저장되는가"이지 "드래그 제스처 구현력"이 아니라고 봤다.

### 불합격 카드는 어디로 갈 수 있는가

지문은 단계를 `서류검토 → 면접 → 처우협의 → 최종합격 / 불합격`으로 적었다.
불합격은 합격 경로의 끝이 아니라 **어느 단계에서나 갈 수 있는 분기**다. 그래서 이전/다음 버튼이 따라갈 경로에서 빼고 별도 버튼으로 뒀다.

문제는 불합격 카드에 버튼이 하나도 없으면 그 카드가 영영 움직이지 않는다는 점이다.

> **가정**: 불합격 카드에는 경로의 첫 단계(`서류검토`)로 되돌리는 버튼만 둔다.
> 불합격 직전에 어느 단계였는지는 저장하지 않으므로 "이전 단계로"가 성립하지 않는다.
> 되돌린 뒤 다시 진행시키는 편이 예측 가능하다고 봤다.

### 이동 규칙을 순수 함수로 분리한 이유

"이 단계에서 어디로 갈 수 있는가"를 `getStageMoves(stage)` 하나로 뽑았다.
컴포넌트 안에 조건문으로 흩어두면 버튼이 맞게 나오는지 화면을 눌러봐야만 알 수 있다.
분리해 두면 단계별 경우의 수를 테스트로 한 번에 확인할 수 있다.

### 옮긴 카드를 대상 컬럼 맨 위로 보낸다

지문에 정렬 요구가 없다. 처음 구현은 `byId[id].stage`만 바꾸고 표시 순서는 건드리지 않았는데,
그러면 옮긴 카드가 **원래 id 순서상 제 자리**에 끼어든다.
실제로 확인해 보니 서류검토 200번째 카드를 면접으로 보냈을 때 227장 중 109번째에 들어갔다.

문제가 세 가지다.

- 옮긴 카드가 화면 밖으로 사라져서 사용자가 이동이 됐는지 알 수 없다
- 낙관적 업데이트에서 치명적이다. **실패 시 카드가 되돌아오는 게 요점인데 카드가 안 보이면 롤백을 확인할 수 없다**
- 스크롤을 한참 내려야 결과를 찾을 수 있어 연속 작업이 어렵다

> **가정**: 방금 옮긴 카드를 대상 컬럼 맨 위에 둔다.
> 실제 칸반 보드가 그렇게 동작하고, 채용 담당자 입장에서도 최근에 처리한 지원자가 위에 있는 편이 자연스럽다.

**mock API 쪽에도 같은 규칙을 넣었다.** 화면에서만 맨 위로 올리면 새로고침했을 때 다시 중간으로 내려가 동작이 어긋난다.
`updateStage`가 해당 지원자를 저장 배열 맨 앞으로 옮긴다. 실제 API가 `updated_at` 내림차순으로 주는 것과 같은 규칙이다.

## D7. 낙관적 업데이트 롤백 전략 — 무엇을 들고 있다가 무엇으로 되돌리는가

요청을 보내기 전에 화면을 먼저 바꾸고, 되돌릴 값을 `pending` 맵에 남긴다.

```ts
pending: Record<string, { previousStage: Stage; previousIndex: number }>
```

**스냅샷에 단계만 담으면 부족하다.** 옮긴 카드를 대상 컬럼 맨 위로 보내기 때문에(D6),
단계만 되돌리면 카드가 원래 컬럼으로 돌아오되 **엉뚱하게 맨 위에 남는다.**
사용자 입장에서는 롤백이 아니라 또 다른 이동처럼 보인다. 그래서 표시 순서상의 위치도 함께 저장한다.

| 시점 | 하는 일 |
|---|---|
| `move/start` | 스냅샷 저장 → 화면의 단계 변경 → 카드를 맨 앞으로 |
| `move/success` | 서버가 준 값으로 맞추고 스냅샷 삭제 |
| `move/failure` | 스냅샷의 단계와 위치로 되돌리고, 스냅샷 삭제, 토스트 표시 |

전체 목록을 통째로 복사해 두지 않은 이유는 1,000건짜리 배열을 이동할 때마다 복제하게 되기 때문이다.
되돌리는 데 필요한 값은 **그 카드 한 장의 단계와 위치뿐**이다.

실제 동작은 [README](README.md#낙관적-업데이트와-롤백)의 화면 녹화(`docs/images/m3-rollback.gif`)에서 확인할 수 있다.
컬럼 개수가 `437/226 → 436/227 → 437/226`으로 왕복하는 것이 낙관적 반영과 롤백이다.

### 사용자에게 알리는 방식

실패를 조용히 되돌리면 사용자는 자기가 잘못 눌렀다고 생각한다. 화면 아래에 토스트를 띄우고 `role="alert"`로 보조기기에도 알린다.
5초 후 자동으로 사라지되 닫기 버튼도 뒀다. 같은 메시지가 연달아 떠도 새 알림으로 인식되도록 토스트에 증가하는 키를 붙였다.

진행 중인 카드는 점선 테두리와 흐린 배경으로 표시한다. **아직 확정되지 않았다는 사실 자체가 정보다.**

### 알려진 한계

`previousIndex`는 이동을 시작한 시점의 배열 위치다. 서로 다른 카드를 거의 동시에 옮기면
나중에 롤백되는 카드의 위치가 한 칸 어긋날 수 있다. 같은 카드를 연속으로 옮기는 경우는 다음 커밋(S1)에서 다룬다.

## D8. 검색·필터 — `useDeferredValue`로 입력과 목록 계산을 분리

요구는 "200건 이상이어도 체감 지연 없이"인데, 이 보드는 1,000건을 다룬다.

병목은 필터링 자체가 아니다. 1,000개를 순회하며 이름을 비교하는 건 1ms 아래다.
**문제는 글자를 칠 때마다 보드 전체가 다시 그려지는 것**이다. 카드 수백 장을 다시 렌더하는 동안 입력이 밀린다.

`useDeferredValue`로 검색어의 사본을 만들어 목록 계산에만 쓴다.
입력창은 즉시 갱신되고, 보드 재계산은 한 박자 늦게 따라온다. 리액트가 급한 렌더(입력)를 먼저 처리한다.

직접 만든 debounce 대신 이걸 쓴 이유는 고정된 대기 시간을 정하지 않아도 되기 때문이다.
빠른 기기에서는 거의 즉시 따라오고 느린 기기에서만 뒤로 밀린다.

> **가정**: 이름 검색은 부분 일치로 처리한다. 앞글자 일치만 허용하면 "민"으로 "김민준"을 찾을 수 없어
> 채용 담당자가 쓰기 불편하다. 대소문자와 앞뒤 공백은 무시한다.

필터가 걸린 상태에서도 단계 이동은 그대로 동작한다. 이동은 `byId`를 바꾸고 필터는 `order`를 걸러낼 뿐이라 서로 간섭하지 않는다.

## D9. 상세 보기 — 카드 본문을 버튼으로 만든다

지문은 "카드 클릭 시 지원자 상세"를 요구한다. 가장 쉬운 구현은 `li`에 `onClick`을 거는 것인데 그렇게 하지 않았다.

`li`에 클릭을 걸면 **마우스로만 열 수 있다.** `Tab`으로 초점이 가지 않고 `Enter`로 눌리지도 않는다.
도전 요구사항에 "키보드만으로 상세 열람"이 있으므로 나중에 `tabIndex`와 키 핸들러를 손으로 붙여야 한다.

카드 본문을 `<button>`으로 감싸면 그 문제가 사라진다. 초점 이동·키 입력·보조기기 역할이 전부 기본으로 따라온다.
이동 버튼들은 그 바깥에 두어 버튼이 겹치지 않게 했다.

### 오버레이로 띄우고, 이동 버튼을 패널 안에 둔다

처음에는 보드 오른쪽에 자리를 만들어 패널을 끼워 넣었다. 보드를 가리지 않으면 상세를 보면서 카드의 이동 버튼을 누를 수 있다고 봤다.

실제로 써 보니 **패널이 열릴 때마다 보드가 320px 밀려 카드가 갑자기 좁아졌다.** 읽고 있던 카드의 위치가 바뀌어 눈이 따라가지 못한다.

패널을 보드 위에 띄우고 뒤에 반투명 배경(`rgba(9, 30, 66, 0.45)`)을 깔았다. 보드 레이아웃은 그대로 유지된다.
배경을 누르면 닫히므로 닫는 방법이 세 가지가 된다 — `Escape`, 닫기 버튼, 배경 클릭.

배경을 덮으면 카드의 이동 버튼을 누를 수 없다. 사이드 패널을 고른 이유가 사라지므로 **이동 버튼을 패널 안으로 옮겼다.**
상세를 확인하고 그 자리에서 단계를 옮기는 흐름이 오히려 더 자연스럽다.

배경을 덮는 형태가 되었으므로 `aria-modal="true"`를 붙이고 `Tab` 초점을 패널 안에 가둔다.
가두지 않으면 보이지 않는 뒤쪽 카드로 초점이 새어 나간다.

### 닫기 아이콘을 직접 그린 이유

`lucide-react` 같은 아이콘 라이브러리를 쓰지 않았다. 필요한 아이콘이 X 하나뿐이다.
UI 라이브러리 없이 마크업을 직접 통제하기로 한 D1과도 어긋난다. 인라인 SVG 열 줄이면 되고, 번들도 늘지 않는다.
아이콘이 여러 개 필요해지면 그때 다시 판단한다.

### 같은 카드를 다시 누르면 닫힌다

토글로 두었다. 열린 카드를 한 번 더 누르면 닫는 동작이 예측 가능하다.

## D10. 경쟁 상태 — 카드별 요청 직렬화 + 스냅샷을 확정 상태로 고정

`optimistic-update`(M3)에서 만든 낙관적 업데이트에는 구멍이 두 개 있었다.

1. **같은 카드를 연속으로 누르면** 요청이 동시에 떠서 서버 도착 순서가 클릭 순서와 달라질 수 있다. 마지막으로 누른 단계가 아닌 값이 저장된다.
2. **스냅샷을 매번 덮어썼다.** 두 번째 클릭이 첫 번째의 낙관적 값(`면접`)을 "이전 상태"로 기록하므로, 실패하면 **서버가 확정한 적 없는 값**으로 되돌아간다.
3. 위치를 인덱스로 기억해서, 그 사이 다른 카드가 앞으로 이동하면 롤백 자리가 한 칸씩 밀렸다.

### 요청을 카드별로 줄 세운다

`createRequestQueue`가 키마다 프라미스 사슬을 만든다. 같은 카드의 요청은 앞 요청이 끝난 뒤에 나간다.
앞 요청이 실패해도 뒤 요청은 이어서 보낸다(`previous.then(task, task)`). 사용자가 누른 의도는 모두 반영되어야 한다.

**화면은 줄을 서지 않는다.** 누르는 즉시 바뀌고, 서버 요청만 차례를 기다린다. 낙관적 업데이트의 이점이 유지된다.

### 응답 순서 비교 대신 직렬화를 택한 이유

"요청에 번호를 붙이고 늦게 도착한 응답은 버린다"는 방법도 있다.
그런데 mock 서버는 **도착 순서대로 덮어쓴다.** 응답을 무시해도 서버에는 이미 그 값이 저장된 뒤다.
화면만 최신으로 보이고 서버는 다른 값을 들고 있게 된다. 직렬화하면 **서버 반영 순서 = 조작 순서**가 보장된다.

### 스냅샷은 첫 요청 때 한 번만

`inFlight`로 진행 중인 요청 수를 센다. 두 번째 클릭은 스냅샷을 덮지 않고 수만 올린다.
응답이 올 때마다 줄이고, **마지막 응답일 때만** 확정하거나 되돌린다.

| 상황 | 결과 |
|---|---|
| 마지막 응답 성공 | 그 값으로 확정 |
| 마지막 응답 실패 | 처음 확정 상태로 롤백 + 알림 |
| 중간 응답 실패, 뒤에 요청 남음 | 되돌리지 않음. 마지막 응답이 최종 상태를 정한다 |

중간 실패를 알리지 않는 이유는, 뒤 요청이 성공하면 서버와 화면이 모두 사용자의 마지막 의도와 맞아서다. 알릴 문제가 없다.

### 위치는 인덱스가 아니라 바로 앞 카드의 id로 기억한다

인덱스는 배열이 바뀌면 의미가 달라진다. 바로 앞에 있던 카드를 기준점으로 삼으면 그 사이 다른 카드가 움직여도 상대 위치가 유지된다.
기준점이 없으면(맨 앞이었으면) 맨 앞으로, 기준점이 사라졌으면 끝으로 보낸다.

## D11. 키보드 접근성 — 조작 가능성이 아니라 연속 작업 가능성

이동 버튼(D6)과 카드 본문 버튼(D9)을 택한 덕분에 **조작 자체는 처음부터 키보드로 가능했다.**
`Tab`으로 초점이 가고 `Enter`로 눌린다. 따로 만든 대체 경로가 없다.

그런데 실제로 키보드만으로 써 보니 한 가지가 막혔다.

**단계를 옮기면 초점이 사라진다.** 카드가 다른 컬럼으로 옮겨가면서 DOM 에서 제거됐다가 다시 생성되고,
초점을 갖고 있던 버튼이 없어지면서 초점이 `body`로 떨어진다.
키보드 사용자는 이동 한 번 뒤에 `Tab`을 처음부터 수백 번 눌러야 다음 카드에 닿는다. 연속 처리가 사실상 불가능하다.

이동을 요청한 카드에 "초점을 돌려달라"는 요청을 남기고, 그 카드가 다시 그려질 때 스스로 초점을 되찾게 했다.

### 요청에 순번을 붙인 이유

대상 id 만 두면 **상관없는 렌더에서도 초점을 빼앗는다.** 검색어를 한 글자 칠 때마다 보드가 다시 그려지는데,
그때마다 마지막으로 옮긴 카드가 초점을 가져가면 입력창에서 글자를 칠 수 없다.

`{ id, seq }` 형태로 두고 이동할 때만 `seq`를 올린다. 카드는 `seq`가 바뀐 렌더에서만 초점을 가져간다.

### 상세 패널이 열려 있는 동안의 초점

패널은 배경을 덮는 형태라 초점을 안에 가둔다(D9). 그래서 **패널이 열린 동안 보드의 카드는 키보드로 닿을 수 없다.**
대신 패널 안에 단계 이동 버튼이 있어서, `Tab`으로 초점을 옮겨 `Enter`로 누르면 된다.

여기에 초점 경로가 세 갈래로 갈린다. 셋 다 어긋나면 키보드 사용자가 길을 잃는다.

| 상황 | 초점이 가야 할 곳 |
|---|---|
| 보드에서 이동 | 옮긴 그 카드 |
| **패널에서 이동** | **패널 안. 보드로 넘기면 화면은 패널인데 초점은 안 보이는 카드에 있게 된다** |
| 패널 닫기 | 열었던 그 카드 |

패널을 닫을 때 **요소가 아니라 카드 id 로** 초점을 되돌린다.
패널에서 단계를 옮기면 그 카드가 다른 컬럼으로 이동하면서 열었던 버튼 자체가 사라지기 때문이다.
사라진 요소를 붙잡고 있으면 되돌릴 곳이 없어 초점이 `body`로 떨어진다.

### 초점 요청은 쓰고 나면 비운다

요청을 남겨 두면 그 카드가 다시 마운트될 때마다 지난 요청으로 초점을 가져간다.
카드는 컬럼을 옮길 때마다 다시 마운트되므로, **상세 패널이 열려 있는데도 뒤쪽 카드가 초점을 빼앗는** 일이 생긴다.
카드가 초점을 받으면 요청을 비우도록 했다.

### 남은 한계 — 1,000장에서의 `Tab` 이동

카드가 1,000장이라 `Tab`만으로 먼 컬럼의 카드까지 가려면 수백 번을 눌러야 한다.
현실적인 우회로는 이름 검색으로 대상을 좁힌 뒤 조작하는 것이고, 검색창은 보드보다 앞에 있어 `Tab` 두 번이면 닿는다.

제대로 풀려면 컬럼 안에서 방향키로 카드를 옮겨 다니고 `Tab`은 컬럼 단위로 건너뛰는 방식(roving tabindex)이 필요하다.
시간 안에 넣지 못했다. [미완료 항목](#미완료-항목) 1번에 남겼다.

## D12. 되돌리기 — 롤백과는 다른 동작이다

이름이 비슷해서 같은 코드로 보이지만 성격이 다르다.

| | 실패 롤백 | 되돌리기 |
|---|---|---|
| 언제 | 서버가 요청을 거절했을 때 | 사용자가 취소를 원할 때 |
| 서버 상태 | 바뀌지 않았다 | **이미 바뀌었다** |
| 하는 일 | 화면을 원래대로 되돌린다 | **반대 방향으로 새 요청을 보낸다** |
| 실패 가능성 | 없다 | **있다. 되돌리기 요청도 15% 확률로 실패한다** |

그래서 되돌리기를 화면에서만 처리하면 안 된다. 서버에 남은 값과 어긋난다.
`undo`는 마지막 이동의 출발 단계를 목표로 하는 **평범한 이동 요청**을 보낸다.
낙관적 반영도, 실패 시 롤백도, 요청 직렬화도 그대로 적용된다.

### 성공한 이동만 기억한다

`move/success` 시점에만 `lastMove`를 남긴다. 화면만 바뀐 상태(`move/start`)를 기억하면
**서버에 존재하지 않는 변경을 취소하려 들게 된다.**

### 되돌리기가 실패하면 대상을 남긴다

처음에는 요청을 보내기 전에 `lastMove`를 비웠다. 그런데 **되돌리기도 15% 확률로 실패한다.**
미리 비우면 실패했을 때 되돌리기 바가 사라져 **다시 시도할 방법이 없어진다.**

요청 전에 비우지 않고, 성공했을 때 `move/success`가 새 대상으로 덮어쓰게 했다.
실패하면 `move/failure`가 `lastMove`를 건드리지 않으므로 바가 그대로 남아 다시 누를 수 있다.

되돌리기가 성공하면 그 되돌리기 자체가 새 대상이 된다(`면접 → 서류검토`). 같은 두 단계를 왕복할 수 있지만,
바에 방향이 표시되므로 무엇이 일어날지는 항상 명확하다. 되돌릴 생각이 없으면 닫기 버튼으로 치운다.

### 실패 메시지를 구분한다

되돌리기도 일반 이동으로 보내지만, 실패 메시지는 `되돌리기를 저장하지 못했습니다.`로 바꾼다.
되돌리기를 눌렀는데 "단계 이동을 저장하지 못했습니다"가 뜨면 사용자는 자기가 무엇을 눌렀는지 헷갈린다.

### 무엇을 되돌리는지 보여준다

버튼에 "되돌리기"만 있으면 무엇이 취소되는지 알 수 없다. 카드가 1,000장이라 더 그렇다.
`임태연 · 서류검토 → 면접`처럼 대상과 방향을 함께 띄운다. `role="status"`로 보조기기에도 알린다.

## D13. 대량 데이터 — 보이는 구간만 그린다

`board-layout`(M1)에서 1,000장을 전부 DOM 에 그려 두고 기준선을 재뒀다.

```
DOM 노드 6,034개 · 스크롤 프레임 중앙값 8.3ms · 최대 311.9ms
```

중앙값은 문제없지만 **최대 311ms 스파이크**가 있다. 스크롤 도중 한 프레임이 그만큼 멈추면 눈에 띄게 끊긴다.

라이브러리를 쓰지 않고 직접 구현했다. 컬럼마다 목록이 따로 스크롤되고 **카드 높이가 모두 같아서**
측정 없이 나눗셈으로 그릴 구간을 구할 수 있다. 이 조건이면 외부 의존성을 더할 이유가 없다.

```
start = scrollTop / 카드높이
end   = start + (보이는 높이 / 카드높이)
```

앞뒤로 6장씩 더 그려서(overscan) 빠르게 스크롤할 때 빈 칸이 보이지 않게 한다.
보이지 않는 구간은 빈 `li` 두 개가 높이만 차지한다. 전체 높이를 미리 잡아 두므로 스크롤바가 실제 건수를 반영한다.

### 카드 높이를 CSS 로 고정했다

가상 스크롤이 높이로 위치를 계산하므로 카드가 내용에 따라 늘어나면 어긋난다.
`CARD_SLOT_HEIGHT` 상수와 CSS 의 `height`를 함께 고정했다. 둘 중 하나만 바꾸면 카드가 겹치거나 벌어진다.

처음에 높이를 120px 로 잡았는데 내용이 118px 이라 **아래 패딩이 10px 이어야 할 자리에 5px 만 남았다.**
이동 버튼이 카드 테두리에 거의 닿았다. 높이를 124px 로 늘리고 본문이 남는 높이를 차지하도록(`margin-top: auto`) 바꿔
위아래 여백이 같아지게 했다. `CARD_SLOT_HEIGHT`도 132px 로 함께 올렸다.

가변 높이를 허용하려면 각 항목을 실제로 재고 누적 오프셋을 관리해야 한다. 이 화면은 카드 내용이 정해져 있어 필요하지 않다.

### 스크롤 처리를 프레임 단위로 묶었다

스크롤 이벤트는 프레임마다 여러 번 들어온다. 들어올 때마다 상태를 바꾸면 렌더가 쌓인다.
`requestAnimationFrame`으로 묶어 프레임당 한 번만 계산한다.

### 결과

```
              이전        이후
DOM 카드      1,000장  →  56장
DOM 노드      6,034    →  637      (-89%)
프레임 중앙값  8.3ms    →  8.3ms
프레임 최대   311.9ms  →  9.4ms    (-97%)
```

중앙값은 그대로고 **최대값이 사라졌다.** 원래 문제가 평균이 아니라 간헐적 스파이크였기 때문이다.

---

## 미완료 항목

시간 안에 처리하지 못했거나, 알면서 남겨 둔 것들이다.

### 1. 1,000장에서의 `Tab` 이동 (S5 관련)

키보드만으로 조작은 되지만 먼 컬럼의 카드까지 `Tab`으로 가려면 수백 번을 눌러야 한다.
현실적인 우회로는 이름 검색으로 대상을 좁힌 뒤 조작하는 것이고, 검색창이 보드보다 앞에 있어 `Tab` 두 번이면 닿는다.

제대로 풀려면 컬럼을 복합 위젯으로 만들어야 한다. `Tab`은 컬럼 단위로 건너뛰고, 컬럼 안에서는 `↑`/`↓`로 카드를 오가는 방식(roving tabindex)이다.
요구사항("키보드만으로 가능") 자체는 충족하지만 실사용 편의는 부족하다. **남은 것 중 가장 먼저 할 작업이다.**

### 2. 초점 요청의 prop 전달 깊이

`focusRequest`와 `onFocusHandled`가 `App → Board → Column → ApplicantCard` 4단계를 타고 내려간다.
Context 로 빼면 중간 단계가 깨끗해지지만 두 가지가 걸렸다.

- 지금 규모(3단 컴포넌트)에서는 prop 으로 내려가는 편이 흐름을 따라가기 쉽다
- Context 는 값이 바뀔 때 구독자 전체가 다시 렌더된다. 카드가 많은 화면에서 불리하다

**모르고 방치한 것이 아니라 재고 나서 남긴 것**이라 여기 적는다. 컴포넌트 층이 한 단계 더 깊어지면 그때 Context 로 옮기는 것이 맞다.

### 3. 드래그앤드롭

지문이 액션 버튼과 택일이라 버튼만 구현했다(D6). 둘 다 제공하면 마우스 사용자의 조작감이 나아진다.
버튼 경로를 유지한 채 얹으면 되므로 접근성이 깨지지는 않는다.

### 4. 상세 패널의 방향키 이동

패널에서 `Tab` + `Enter`로 단계를 옮길 수 있지만, 이동할 때마다 버튼 구성이 바뀌어 `Tab` 횟수가 달라진다.
`←`/`→`로 단계를 오갈 수 있으면 연속 조작이 편해진다. 요구사항은 이미 충족해서 우선순위를 뒤로 뒀다.

### 5. 카드 높이 고정 제약

가상 스크롤이 높이로 위치를 계산하므로 `CARD_SLOT_HEIGHT` 상수와 CSS 의 `height`가 짝으로 묶여 있다(D13).
한쪽만 바꾸면 카드가 겹치거나 벌어진다. 양쪽에 주석을 남겼지만 구조적으로 막지는 못했다.

각 항목을 실제로 재고 누적 오프셋을 관리하면 가변 높이를 허용할 수 있다. 이 화면은 카드 내용이 정해져 있어 필요하지 않다고 봤다.

### 6. 배포

지문에서 선택 항목이라 하지 않았다. 정적 빌드라 Vercel 등에 그대로 올릴 수 있다.
